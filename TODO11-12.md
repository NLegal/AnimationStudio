# TODO — Phase 11–12 Audit Gaps

Audit of PHASE11.md and PHASE12.md against the implemented code in
`src/publishing/` and `src/studio/`. Spec-mandated systems that were missing,
stubbed, or incomplete are documented below with the fix that closes each gap.

**Status: all 22 gaps closed.**

---

## Phase 11 — Publishing, Distribution & Channel Management System

### Complete
- `src/publishing/` — models, metadata engine (title/description/keywords),
  branding/compliance, playlist/scheduling, thumbnail strategy, content variant
  planning, localization, publishing engine (approve/schedule/publish/update/
  archive), analytics, archive, channel manager, notifications, lifecycle
- `Publishing/` storage structure + PUBLISHING_GUIDE.md

### Gaps Fixed

| # | Gap | Priority | Type | Fix |
|---|-----|----------|------|-----|
| 1 | Content Variants missing Facebook Clip, Thumbnail, Preview Images, Newsletter Assets, Website Assets, Behind-the-Scenes (spec: "each episode should automatically generate" 12 variants; only 7 present) | HIGH | Code | `thumbnail.py` `ContentVariantEngine` — add `facebook_clip`, `thumbnail`, `preview`, `newsletter`, `website`, `bts` specs |
| 2 | Thumbnail evaluation missing the "Emotion" dimension (spec lists eye contact, expression, brightness, contrast, readability, emotion, educational focus) | MED | Code | `thumbnail.py` `ThumbnailStrategy` — add `emotion` weight + parameter |
| 3 | Scheduling Engine ignores `language` and `embargo_date` even though the model stores them (spec: schedule stores publish date/time/timezone/platform/language/visibility/premiere/embargo) | MED | Code | `schedule.py` `SchedulingEngine.create_schedule` — accept and persist both |
| 4 | Release Calendar / publishing cadence missing (spec: daily, weekly, bi-weekly, monthly, holiday specials, season launches, premieres; consistent cadence) | HIGH | Code | `schedule.py` — new `ReleaseCalendar` |
| 5 | LocalizedPublication missing audio tracks (spec: translated titles, descriptions, tags, subtitles, audio tracks, localized thumbnails, localized end screens) | MED | Code | `localization.py` — add `audio_track` field |
| 6 | Compliance missing platform-policy and community-guidelines checks (spec: verify COPPA, platform policies, age-appropriate metadata, copyright, music licensing, thumbnail compliance, community guidelines) | MED | Code | `compliance.py` `ComplianceEngine` — add `check_platform_policies` + `check_community_guidelines` |
| 7 | Branding verification missing social links (spec: every upload includes studio logo, series branding, colors, typography, intro, outro, end screen, website, social links) | MED | Code | `compliance.py` `BrandingEngine.verify_branding` — add `has_social_links` |
| 8 | Performance analytics snapshot incomplete (spec collects playlists, traffic sources, geography, language, device type, platform, episode revenue in addition to the 10 present fields) | HIGH | Code | `models.py` `AnalyticsSnapshot` — add the 7 missing fields |
| 9 | Educational analytics is a generic passthrough; no dedicated tracking (spec: learning topic, target age, episode length, song count, question count, interactive moments, completion rate, replay rate, topic popularity) | HIGH | Code | `analytics.py` — new `EducationalAnalyticsEngine` |
| 10 | `conclude_ab_test` never records a winner (always keeps `variant_a`) | MED | Code | `analytics.py` — add `winner` field to `ABTest` and set it on conclude |
| 11 | Archive entries omit subtitles (spec archive policy stores master video, published versions, metadata, thumbnail, description, tags, subtitles, analytics snapshot, publishing history) | MED | Code | `archive.py` `store()` — accept and persist subtitles |
| 12 | Publishing Dashboard missing (deliverable) | HIGH | Code | `dashboard.py` — new `PublishingDashboard` |
| 13 | No end-to-end publishing validation before release (spec quality checklist: "Publishing validation passed") | MED | Code | `publishing.py` `PublishingEngine` — add `validate_ready()` |

### Notes
- "Suggested APIs" are integration suggestions exposed to the automation layer
  and are covered by Phase 12 orchestration (see Phase 12 gap 7).
- "Recommended operational dashboard" is implemented as part of gap 12.

---

## Phase 12 — Studio Automation, AI Orchestration & Autonomous Production Platform

### Complete
- `src/studio/` — models, workflow engine (resumable steps, episode factory),
  event bus, task queue (retries/priorities/dependencies), agent registry
  (16 agents), worker pool (7 worker types), scheduler (daily/weekly/monthly),
  model/prompt/asset registries, quality gate, resource manager, render farm,
  monitoring/alerts, production tracker, learning loop, analytics, orchestrator
- `Studio/` storage structure + STUDIO_ORCHESTRATION_GUIDE.md

### Gaps Fixed

| # | Gap | Priority | Type | Fix |
|---|-----|----------|------|-----|
| 14 | Prompt Registry missing model compatibility and performance metrics (spec stores prompt, version, author, model compatibility, performance metrics, revision history, approval status) | MED | Code | `registry.py` `PromptRegistry` — accept + persist `model_compatibility` and `performance` |
| 15 | Error Recovery missing fallback models, fallback prompts, checkpoint resume, manual approval, escalation (only automatic retries exist) | HIGH | Code | `recovery.py` — new `ErrorRecoveryEngine` |
| 16 | Security / RBAC + audit logging missing (spec: protect assets, characters, datasets, prompt library, model registry, credentials, publishing accounts; role-based access control + audit logging) | HIGH | Code | `security.py` — `AccessControl`, `AuditLog`, `SecurityManager` |
| 17 | Backup Strategy and Disaster Recovery missing (deliverable: "Backup and disaster recovery framework"; spec: versioned restores, recovery targets, documented + tested procedures) | HIGH | Code | `backup.py` — `BackupManager` + `DisasterRecovery` |
| 18 | Plugin architecture missing (deliverable; spec: image/video/music/voice models, publishing platforms, analytics providers, translation engines, storage providers, rendering backends replaceable via interfaces) | MED | Code | `plugins.py` — `PluginRegistry` |
| 19 | Operations Dashboard missing (deliverable; spec monitoring dashboard: production queue, GPU status, worker status, publishing queue, localization status, failures, render time, episode status, revenue, subscriber growth, channel health) | MED | Code | `dashboard.py` — new `StudioDashboard` |
| 20 | Unified API platform missing (deliverable; spec API architecture: studio, creative, production, rendering, publishing, analytics, automation, administration) | HIGH | Code | `api.py` — new `StudioAPI` |
| 21 | Quality Gate not preloaded with the 10 validation domains (spec: validate stories, images, animations, audio, videos, metadata, publishing, localization, branding, accessibility) | MED | Code | `quality.py` — `register_domain_checkers()` wired into orchestrator defaults |
| 22 | Resource snapshot only reports GPU/CPU/RAM (spec resource manager tracks GPU, CPU, RAM, VRAM, disk, bandwidth, power) | LOW | Code | `resources.py` — snapshot all 7 fields |

### Notes
- "Knowledge Base" is a documentation requirement ("the studio should be
  self-documenting") satisfied by the existing guide files; no code system
  mandated, so left out.
- Scalability, GPU distribution, and API-surface descriptions are architectural
  principles realized by the existing registry/orchestrator/farm abstractions.
