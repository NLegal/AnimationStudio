---
phase: 07-music-generation-backend-integration
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []            # ROADMAP Phase 7 "Requirements: TBD" (no REQUIREMENTS.md mapping) — success criteria sourced goal-backward from ROADMAP Phase 7 goal per planning brief
requirements_addressed: []  # same as above; coverage carried by must_haves
user_setup: []
files_modified:
  - src/music_generation/__init__.py
  - src/music_generation/models.py
  - src/music_generation/backends.py
  - src/music_generation/mock.py
  - tests/test_music_generation.py

must_haves:
  truths:
    - "MockBackend.generate() returns byte-identical MusicResult.audio for identical seeds, computed entirely offline (no network, no audio hardware)"
    - "MusicGenerationBackend is a @runtime_checkable typing.Protocol and MockBackend satisfies isinstance checks WITHOUT inheriting from it"
    - "Category music parameters resolve exactly per the locked research table (Bedtime → 66 BPM, F major, 3/4, instrumental-intro lyric scaffold, 120 s)"
    - "Song captions originate exclusively from src.audio_bible.prompts.build_music_prompt so Phase 5 bible rules stay authoritative"
    - "tests/test_music_generation.py runs green offline in under 30 s (pytest timeout already configured) with zero network I/O and zero writes to the repo-root asset-catalog database"
  artifacts:
    - path: src/music_generation/__init__.py
      provides: "Package init, phase-scoped docstring, public exports (models, protocol, exceptions, MockBackend)"
    - path: src/music_generation/models.py
      provides: "MusicRequest, MusicStatus, MusicResult (Pydantic v2, mirroring src/models/schemas.py conventions)"
    - path: src/music_generation/backends.py
      provides: "MusicGenerationBackend Protocol, MusicBackendError + NotConfigured/BackendUnavailable/GenerationFailed, CategoryMusicParams + CATEGORY_MUSIC_PARAMS + resolve_music_params + build_music_request, transport seam (_post_json/_get_json/_get_bytes/_urlopen/_sleep), generate() orchestration loop"
    - path: src/music_generation/mock.py
      provides: "Deterministic MockBackend with latency/failure/state-count injection knobs and valid RIFF/WAV synthesis"
    - path: tests/test_music_generation.py
      provides: "Offline pytest classes: TestProtocolAndExceptions, TestCategoryMapping, TestMockBackend, TestTransportSeam, TestGenerateOrchestration"
  key_links:
    - "backends.build_music_request → models.MusicRequest, with numeric params from CATEGORY_MUSIC_PARAMS and caption rules delegated to audio_bible.prompts.build_music_prompt"
    - "MusicGenerationBackend.generate default method → the concrete backend's submit/poll/download (MockBackend now; AceStepBackend lands in plan 07-02)"
    - "transport helpers' typed error map (connection→BackendUnavailable, 401/403→NotConfigured, failed/malformed→GenerationFailed) is consumed unchanged by AceStepBackend in plan 07-02"

---

<assumption_delta_decision>
<!-- assumption-delta scan fired: kind=optional, term "optional third-party wrapper adapter" (ROADMAP Phase 7) -->
noun: "music generation backend (the MusicGenerationBackend protocol resolved via get_backend)"
decision: promote
rationale: >
  This phase introduces the generalized abstraction natively — protocol + typed
  result models + name-based registry arrive together with three variants
  (ace-step, suno, mock), so there is no legacy singular anchor to demote. The
  detector's `optional` signal (SunoWrapperBackend) is a fourth variant behind
  the SAME protocol, not a second identity anchor: it is flagged experimental,
  excluded from default registry resolution, and reachable only by explicit
  import/construction. Promote is recorded rather than add-alongside because the
  protocol is the primary noun from day one.
invariant_test: >
  Accepted — encoded in plan 07-02 acceptance criteria: get_backend("suno")
  resolves the refusing stub, never the experimental wrapper; the wrapper is
  absent from default registry resolution.
</assumption_delta_decision>

<objective>
Build the provider-agnostic core of `src/music_generation/`: typed request/status/result models, the runtime-checkable `MusicGenerationBackend` protocol, the typed exception taxonomy, the category→music-parameter mapping anchored on Phase 5 bible standards, the stdlib transport seam, and the deterministic mock backend — proven end-to-end by an offline tracer before expansion.

Purpose: This is the substrate every Phase 8 wiring point (pipeline mode, Review UI hooks, Colab notebook) will program against; the protocol, error map, and golden parameter values are LOCKED design decisions (RESEARCH §2–§5) and must not drift.

Output: 5 files — `src/music_generation/{__init__,models,backends,mock}.py` + `tests/test_music_generation.py`, all green offline.
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-music-generation-backend-integration/07-RESEARCH.md
@.planning/phases/07-music-generation-backend-integration/07-PATTERNS.md
@.planning/phases/07-music-generation-backend-integration/07-VALIDATION.md
@.planning/phases/07-music-generation-backend-integration/COVERAGE.md
@src/models/schemas.py
@src/generation_engine/base.py
@src/generation_engine/mock_backend.py
@src/pipeline/job_queue.py
@src/audio_bible/prompts.py
@pyproject.toml
</context>

## Hard Constraints (non-negotiable, echo in every commit)

- **C1** — No visual-asset generation anywhere in this phase; no audio generated in CI/tests (mock synthesizes tiny deterministic PCM data structures, not rendered audio).
- **C2** — The repo-root asset-catalog SQLite database is NEVER opened, written, or imported by this phase's code or tests; results stay in memory.
- **C3** — No network I/O in tests — fake transport injection only; the suite must pass with all interfaces down.
- **C4** — stdlib `urllib.request` only; NO new dependencies (third-party HTTP clients are not declared in pyproject and must not be added).
- **C5** — Env config surface: `ACESTEP_API_KEY`, `ACESTEP_BASE_URL` (default `http://localhost:8001`), `MUSIC_BACKEND`.
- **C6** — RESEARCH.md §2–§5 are LOCKED design decisions: REST contract, error mapping, protocol surface, category table, timeouts/polling. Implement verbatim; do not redesign.

## Artifacts this phase produces (plan 01 share)

New package `src/music_generation/` with symbols: `MusicRequest`, `MusicStatus`, `MusicResult`; `MusicGenerationBackend` (Protocol); `MusicBackendError`, `NotConfigured`, `BackendUnavailable`, `GenerationFailed`; `CategoryMusicParams`, `CATEGORY_MUSIC_PARAMS`, `resolve_music_params`, `build_music_request`, `music_negative_prompt`; transport seam functions `_post_json`, `_get_json`, `_get_bytes` plus module seams `_urlopen` and `_sleep`; `MockBackend`. New test module `tests/test_music_generation.py`. (Plan 07-02 adds `AceStepBackend`, `SunoBackend`, `SunoWrapperBackend`, `get_backend`, and the CLI.)

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 [validation 07-01-01 + 07-01-03 seed]: Tracer — deterministic end-to-end generation through the core layer</name>
  <files>src/music_generation/__init__.py, src/music_generation/models.py, src/music_generation/backends.py, src/music_generation/mock.py, tests/test_music_generation.py</files>
  <read_first>
  - .planning/phases/07-music-generation-backend-integration/07-RESEARCH.md — §4 (protocol surface, models, exceptions, mock contract), §5 (timeouts/polling defaults)
  - .planning/phases/07-music-generation-backend-integration/07-PATTERNS.md — assignments for models.py, backends.py, mock.py, __init__.py + all four Deliberate Divergences
  - src/models/schemas.py — Pydantic v2 conventions to mirror (Literal enums, Optional, Field(default_factory), bounds, per-class docstrings)
  - src/generation_engine/base.py — interface/input/output/exception SHAPE to copy, swapping ABC for typing.Protocol per divergence #1
  - src/pipeline/job_queue.py lines 14–24, 92–115 — state-machine vocabulary (pending|running|completed|failed) and domain-error message style to echo in GenerationFailed payloads
  - src/generation_engine/mock_backend.py — deterministic seed-derived placeholder precedent (docstring contract + hash derivation)
  - src/generation_engine/__init__.py and src/audio_bible/__init__.py — export-list + phase-scoped module-docstring style
  - tests/test_audio_bible.py lines 1–29 — test-file header, ROOT constant, fixture conventions
  </read_first>
  <behavior>
    - isinstance(MockBackend(), MusicGenerationBackend) is True via @runtime_checkable Protocol, with MockBackend NOT inheriting from the protocol
    - MusicRequest(category="Bedtime", topic="sleepy moon", seed=7) accepted; MusicStatus(state="pending").progress defaults to 0.0; MusicResult.format defaults to "wav"; invalid state string on MusicStatus raises pydantic ValidationError
    - m = MockBackend(); r1 = m.generate(req); r2 = m.generate(req) with same req/seed → r1.audio == r2.audio byte-for-byte, r1.backend == "mock", r1.seed == effective seed used
    - r1.audio starts with RIFF magic bytes and parses as a structurally valid tiny WAV (44-byte canonical header layout)
    - MockBackend(fail_submit=True).generate(req) raises GenerationFailed; a backend configured with states_before_complete=2 reports pending→running→completed across poll() calls
    - NotConfigured, BackendUnavailable, GenerationFailed are each subclasses of MusicBackendError
  </behavior>
  <action>
Create package `src/music_generation/` (new directory; nothing exists today). Write five thin vertical pieces and ONE end-to-end offline test proving the slice — production quality, committed for keeps.

1. `__init__.py` — module docstring opening "Phase 7 — Music Generation Backend Integration …" (copy the phrasing style of `src/audio_bible/__init__.py`); export and list in `__all__` exactly: MusicRequest, MusicStatus, MusicResult, MusicGenerationBackend, MusicBackendError, NotConfigured, BackendUnavailable, GenerationFailed, MockBackend. Do NOT export adapter names yet — AceStepBackend/SunoBackend/get_backend arrive in plan 07-02.

2. `models.py` — Pydantic v2 BaseModels mirroring schemas.py conventions (per-class docstring, Literal for enums, Optional[T]=None, Field(default_factory)/bounds):
   - MusicRequest: category: str; topic: str = ""; duration_s: int = Field(default=60, ge=10, le=600); vocals: str = "female lead vocal, children's choir" (same default as prompts.py:189); lyrics_override: Optional[str] = None; seed: Optional[int] = None; tags: list[str] = Field(default_factory=list). Exactly these seven fields — RESEARCH §4 fixes the surface.
   - MusicStatus: state: Literal["pending","running","completed","failed"]; progress: float = Field(default=0.0, ge=0.0, le=1.0); error: Optional[str] = None.
   - MusicResult: request: MusicRequest; audio: bytes; format: Literal["wav","mp3"] = "wav"; job_id: str; backend: str; seed: int.

3. `backends.py` — core contracts (mapping table itself is Task 2; keep this task focused):
   - Exception taxonomy with explanatory docstrings stating raise-conditions per RESEARCH §2: MusicBackendError(Exception) base; NotConfigured (missing credential / unauthorized / refusing stub); BackendUnavailable (connection refused, DNS failure, timeout); GenerationFailed (job failed status, malformed response, orchestration timeout).
   - @runtime_checkable class MusicGenerationBackend(Protocol) with methods: is_configured() -> bool; submit(request: MusicRequest) -> str; poll(job_id: str) -> MusicStatus; download(job_id: str) -> bytes; and default method generate(request: MusicRequest, *, timeout_s: float = 300.0, poll_interval_s: float = 2.0) -> MusicResult implementing the submit→poll→download convenience loop (full backoff/timeout hardening lands in Task 3 — this task ships a correct minimal loop calling self.submit/self.poll/self.download and raising GenerationFailed on terminal failed state).
   - Module seams for later testability: `_sleep(seconds)` delegating to time.sleep, and `_urlopen` aliasing urllib.request.urlopen — all delay/network flows must route through these two names (constraint C3/C4).

4. `mock.py` — MockBackend implementing the protocol by duck-typing (no inheritance):
   - Constructor knobs: latency_s: float = 0.0, fail_submit: bool = False, states_before_complete: int = 0.
   - Class docstring documents the determinism contract exactly in the spirit of mock_backend.py: same effective seed ⇒ byte-identical output.
   - Effective seed = request.seed or constructor seed argument or 0; drive sample synthesis from random.Random(effective_seed).
   - Synthesize a tiny valid mono 16-bit 8 kHz WAV: 44-byte canonical RIFF header (RIFF/WAVE/fmt /data chunks, correct size fields) + ~2000 samples alternating seed-derived sine segments and silence. This is a data-structure exercise for offline parsing — NOT audio rendering (constraint C1).
   - poll(): returns MusicStatus walking pending→running→completed across states_before_complete calls, then completed with progress 1.0; latency_s honored via _sleep. download(): returns the synthesized bytes. fail_submit=True makes submit raise GenerationFailed.
   - backend name reported in MusicResult: "mock".

5. `tests/test_music_generation.py` — header/docstring/ROOT constant modeled on tests/test_audio_bible.py lines 1–29. Create class TestMockBackend with the tracer test `test_generate_end_to_end_deterministic` (build MusicRequest Bedtime/120 s/seed 7, two generate() runs byte-equal, RIFF magic asserted) plus the <behavior> cases above (protocol isinstance, pydantic defaults/validation-error, fail_submit raise, state walk, exception subclass assertions may live here initially and migrate to TestProtocolAndExceptions in Task 3).

Commit the tracer as its own atomic commit before starting Task 2.
  </action>
  <verify>
    <automated>python -m pytest tests/test_music_generation.py::TestMockBackend -x -q && python -m pytest tests/test_music_generation.py -q</automated>
  </verify>
  <acceptance_criteria>
  - `python -m pytest tests/test_music_generation.py -q` exits 0 with all tests passing in well under the 30 s pytest timeout
  - TestMockBackend::test_generate_end_to_end_deterministic proves same-seed byte equality AND RIFF magic on the returned bytes
  - `python -c "import sys; sys.path.insert(0,'.'); from src.music_generation import MusicGenerationBackend, MockBackend, MusicBackendError, NotConfigured, BackendUnavailable, GenerationFailed"` succeeds
  - `python -c "import sys; sys.path.insert(0,'.'); from src.music_generation.mock import MockBackend; from src.music_generation.backends import MusicGenerationBackend; import inspect; assert not issubclass(MockBackend, MusicGenerationBackend); assert isinstance(MockBackend(), MusicGenerationBackend)"` succeeds (protocol conformance WITHOUT inheritance)
  - `! rg -q "sqlite3" src/music_generation/` (constraint C2 — no database access in the package)
  - `! rg -qi "\bimage" src/music_generation/` (constraint C1 — no visual-asset concerns in this package)
  - git log shows the tracer committed separately from expansion work
  </acceptance_criteria>
  <reversibility rating="reversible">Additive new package + test file; protocol-over-ABC choice is a mechanical refactor if ever revisited.</reversibility>
  <done>The single happy path — construct MusicRequest → MockBackend.generate() → byte-identical valid-WAV MusicResult — works end-to-end, fully offline, and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 [validation 07-01-02]: Category → music-parameter mapping anchored on Phase 5 bible standards</name>
  <files>src/music_generation/backends.py, tests/test_music_generation.py</files>
  <read_first>
  - .planning/phases/07-music-generation-backend-integration/07-RESEARCH.md §3 — the LOCKED category table (caption seeds, bpm, key_scale, time_signature, lyric structure) and duration guidance (45–90 s typical; Bedtime 120 s)
  - src/audio_bible/prompts.py lines 23, 175–215 — AUDIO_NEGATIVE_BASE, category_negative(), build_music_prompt() signature and template fallback chain (reuse, never re-implement)
  - src/audio_bible/libraries.py lines 30–34, 70 — PRIMARY_MUSIC_PLATFORM/SECONDARY_MUSIC_PLATFORM constants and SONG_CATEGORIES (case handling reference)
  - tests/test_audio_bible.py lines 109–121 — TestMusicBrief golden-value assertion style to mirror
  </read_first>
  <behavior>
    - resolve_music_params("Bedtime") → bpm 66, key_scale "F major", time_signature "3/4", lyric_structure starting "[instrumental intro]", duration_s 120
    - resolve_music_params("Alphabet") → 110 / "C major" / "4/4"; ("Numbers") → 116 / "D major" / "4/4"; ("Colors") → 108 / "G major" / "4/4" with bridge-containing scaffold; ("Animals") → 120 / "C major" / "4/4"
    - Case-insensitive lookup: resolve_music_params("bedtime") equals resolve_music_params("Bedtime")
    - Unknown category ("Spaceship") falls back to the generic row (bpm 110, "C major", "4/4", verse-chorus scaffold, duration_s 60)
    - build_music_request(category="Bedtime", topic="sleepy moon", seed=42) returns MusicRequest with duration_s 120 and seed 42; explicit duration_s=90 overrides the category default
    - Caption property: build_music_prompt(category, topic, ...) output is ≤ 512 characters and contains the category's prompt keyword (verified for all five categories)
  </behavior>
  <action>
Extend `src/music_generation/backends.py` with the locked category mapping (RESEARCH §3 verbatim) and the request builder; extend `tests/test_music_generation.py` with class TestCategoryMapping BEFORE implementing, then make it green.

1. CategoryMusicParams(BaseModel) in backends.py: bpm: int; key_scale: str; time_signature: str; lyric_structure: str; duration_s: int; caption_keyword: str — with a docstring noting values are locked by RESEARCH §3.

2. Module constant CATEGORY_MUSIC_PARAMS with EXACTLY these rows (bpm/key/time-signature/lyric-scaffold/caption-keyword verbatim from RESEARCH §3):
   - Alphabet — 110, "C major", "4/4", "[verse][chorus][verse][chorus]", caption_keyword "alphabet"
   - Numbers — 116, "D major", "4/4", "[verse][chorus][verse][chorus]", caption_keyword "counting"
   - Colors — 108, "G major", "4/4", "[verse][chorus][bridge][chorus]", caption_keyword "colors"
   - Animals — 120, "C major", "4/4", "[verse][chorus][verse][chorus]", caption_keyword "animal"
   - Bedtime — 66, "F major", "3/4", "[instrumental intro][verse][chorus][verse][outro]", caption_keyword "lullaby"
   Duration defaults (planner discretion inside RESEARCH §3's stated 45–90 s band, with Bedtime pinned at 120 s by research — record this choice in the SUMMARY): Alphabet 75, Numbers 75, Colors 60, Animals 80, Bedtime 120.
   Plus a "generic" fallback row mirroring the base-template path of build_music_prompt: 110, "C major", "4/4", "[verse][chorus][verse][chorus]", duration_s 60, caption_keyword derived from the category slug at build time.

3. resolve_music_params(category: str) -> CategoryMusicParams — case-insensitive exact-name match against the five categories, else the generic fallback.

4. build_music_request(category, topic="", *, vocals="female lead vocal, children's choir", mood="", seed=None, lyrics_override=None, tags=None, duration_s=None) -> MusicRequest — resolves duration via category params when duration_s is None; passes everything else straight into MusicRequest. IMPORTANT (locked decision, RESEARCH §3): captions are NOT baked into this builder — adapters compose captions by calling src.audio_bible.prompts.build_music_prompt so bible ordering rules stay authoritative; the table supplies only numeric params + lyric scaffolds.

5. music_negative_prompt(category: str) -> str — thin wrapper returning category_negative(category, include_base=True) from prompts.py (pure delegation; zero re-implementation).

6. TestCategoryMapping in tests/test_music_generation.py covering every <behavior> bullet, using the golden-value style of tests/test_audio_bible.py TestMusicBrief.

Commit after green.
  </action>
  <verify>
    <automated>python -m pytest tests/test_music_generation.py::TestCategoryMapping -q</automated>
  </verify>
  <acceptance_criteria>
  - `python -m pytest tests/test_music_generation.py::TestCategoryMapping -q` exits 0
  - Bedtime golden values asserted exactly: bpm 66, key_scale "F major", time_signature "3/4", duration_s 120, lyric_structure beginning "[instrumental intro]"
  - All five category rows asserted against the research table values listed in <action>; generic fallback asserted for an unknown category
  - Caption-length property asserted ≤ 512 chars for all five categories via src.audio_bible.prompts.build_music_prompt
  - `python -m pytest tests/ -q` remains fully green (sampling rate per 07-VALIDATION.md: full suite after each wave task)
  </acceptance_criteria>
  <done>Every Phase 5 song category resolves to its locked numeric params + lyric scaffold offline, and request construction delegates caption authority to the audio bible.</done>
</task>

<task type="auto">
  <name>Task 3 [validation 07-01-03 completion]: Transport seam + generate() orchestration hardening</name>
  <files>src/music_generation/backends.py, tests/test_music_generation.py</files>
  <read_first>
  - .planning/phases/07-music-generation-backend-integration/07-RESEARCH.md §4 transport-seam bullet and §5 timeouts/polling (connect 5 s, read 30 s; overall timeout_s 300; backoff 1→2→4→8 s capped; poll interval 2.0 s; mock immediate)
  - .planning/phases/07-music-generation-backend-integration/07-PATTERNS.md — "No Analog Found" rows (orchestration w/ backoff-cap; transport seam) follow RESEARCH spec; ace_step.py assignment notes the transport divergence plan 07-02 will consume
  - src/generation_engine/cloud_backend.py lines 232–248 and 290–318 — poll-loop and bounded-timeout-guard shapes being adapted (do NOT copy their lazy third-party-client imports or log-and-return-empty degradation)
  - tests/test_scoring_plugins.py around line 194 and tests/test_generation_engine.py lines 86–92 — monkeypatch/env-isolation precedents
  </read_first>
  <behavior>
    - _post_json/_get_json/_get_bytes pass URL, method, headers, and timeout tuple (5, 30) to the module-level _urlopen seam; JSON responses decode to dict; binary endpoints return bytes
    - Non-2xx status surfaces as the mapped typed exception; connection-level failures (URLError, socket.timeout, OSError) surface as BackendUnavailable
    - generate() with states_before_complete=2 sleeps with the exponential sequence starting 1 s then 2 s (cap ×8 thereafter) via the _sleep seam — verified by counting monkeypatched sleep calls, zero real waiting
    - generate() raises GenerationFailed once the monotonic deadline (timeout_s) is exceeded; message contains job id and elapsed seconds
    - Terminal failed poll state raises GenerationFailed including the server-provided error text
    - Exception taxonomy: NotConfigured/BackendUnavailable/GenerationFailed all subclass MusicBackendError (TestProtocolAndExceptions)
  </behavior>
  <action>
Harden the core loop and formalize the transport seam in `src/music_generation/backends.py`; cover with three new pytest classes in `tests/test_music_generation.py`.

1. Transport seam (module-level, stdlib-only per constraint C4):
   - `_post_json(url, payload, headers, timeout_s)` → dict; `_get_json(url, headers, timeout_s)` → dict; `_get_bytes(url, headers, timeout_s)` → bytes. Each builds a urllib Request, routes through the module-level `_urlopen` seam (introduced in Task 1), uses the explicit timeout tuple (connect 5 s, read 30 s) per RESEARCH §5, decodes JSON with the stdlib json module (strict parse; malformed body → GenerationFailed).
   - Error mapping AT THE SEAM (single choke point reused by plan 07-02's adapter): URLError/socket.timeout/OSError → BackendUnavailable; HTTP 401/403 → NotConfigured; other HTTP errors → GenerationFailed. Never log or embed header values (including Authorization) in exception messages — see threat T7-01-I.
   - Expose `DEFAULT_TRANSPORT` verbatim as `SimpleNamespace(post_json=_post_json, get_json=_get_json, get_bytes=_get_bytes)` (from types import SimpleNamespace) — these three attribute names are PINNED exactly like this because plan 07-02 adapters consume them as self._transport.post_json / self._transport.get_json / self._transport.get_bytes when accepting a `transport=` argument defaulting to it.

2. generate() hardening in the protocol default method:
   - Replace any real time.sleep/time.monotonic use with the `_sleep` seam and time.monotonic read through a module-level `_monotonic()` helper so tests patch both without waiting.
   - Poll cadence: poll_interval_s between attempts, doubling each retry (1→2→4→8 s) capped at ×8 of the base interval, until MusicStatus.state is terminal or the monotonic deadline (timeout_s=300 default) passes.
   - Deadline exceeded → GenerationFailed(message includes job_id and elapsed). Terminal failed state → GenerationFailed including status.error text. Completed → download() and assemble MusicResult with backend-specific name, effective seed, and format.

3. Tests (offline, monkeypatched seams only — constraint C3):
   - TestTransportSeam: fake _urlopen returning scripted response stand-ins (read()/status/headers); assert exact URL, method, headers dict, and (5, 30) timeout reach the seam for all three helpers; JSON and bytes decode paths.
   - TestGenerateOrchestration: MockBackend(states_before_complete=2) with patched _sleep counter asserting the 1 s then 2 s backoff sequence; patched _monotonic forcing deadline expiry → fast GenerationFailed (suite stays < 30 s); failed-terminal script → GenerationFailed carrying error text.
   - TestProtocolAndExceptions: subclass taxonomy + runtime_checkable isinstance conformance for MockBackend.

Run the FULL suite (`python -m pytest tests/ -q`) as the wave-gate check per 07-VALIDATION.md sampling rates. Commit after green.
  </action>
  <verify>
    <automated>python -m pytest tests/test_music_generation.py::TestTransportSeam tests/test_music_generation.py::TestGenerateOrchestration tests/test_music_generation.py::TestProtocolAndExceptions -q && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - `python -m pytest tests/ -q` exits 0 — entire repo suite green offline (wave-gate sampling per 07-VALIDATION.md)
  - Backoff sequence 1 s → 2 s (doubling, cap ×8) asserted via _sleep call counts with zero real sleeping
  - Deadline expiry produces GenerationFailed naming job id and elapsed time, raised in milliseconds of wall-clock time thanks to the patched monotonic seam
  - All three transport helpers demonstrably route through the `_urlopen` seam (fakes intercept every call; no direct network syscalls possible in tests)
  - `! rg -q "import requests" src/music_generation/` (constraint C4 — stdlib transport only)
  </acceptance_criteria>
  <done>Core layer complete: protocol, exceptions, mapping, transport seam, hardened generate() loop, deterministic mock — full repo suite green offline, ready for plan 07-02 adapters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process ↔ local ACE-Step HTTP service | Untrusted/malformed JSON crosses here once adapters wire in (plan 07-02); seam + error map established now |
| environment ↔ process | ACESTEP_API_KEY / ACESTEP_BASE_URL / MUSIC_BACKEND enter as untrusted-ish strings |
| tests ↔ world | CI must have ZERO network/audio-hardware/database side effects |

## STRIDE Threat Register (ASVS L1; block on unresolved high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T7-01-SC | Tampering (supply chain) | dependency set of src/music_generation | high | mitigate | Constraint C4 enforced and grep-gated: stdlib transport only, zero new dependencies declared or imported |
| T7-01-T | Tampering | _get_json/_post_json response decoding | medium | mitigate | Strict stdlib json parsing; malformed body → typed GenerationFailed; no dynamic evaluation of payload content |
| T7-01-I | Info Disclosure | transport helpers / exception messages | high | mitigate | Helpers never log or embed header values (incl. Authorization) in messages or metadata; gate extended in plan 07-02 (T7-02-I covers Bearer token serialization) |
| T7-01-D | Denial of Service | generate() polling loop | medium | mitigate | Hard deadline timeout_s=300, bounded exponential backoff capped ×8, per-call (5, 30) connect/read timeouts |
| T7-01-C | Database integrity | entire package vs repo-root asset-catalog DB | high | mitigate | Constraint C2 grep-gated: no database driver imports or file access anywhere in src/music_generation/ ; results in-memory only (persistence is explicitly Phase 8 scope) |
| T7-01-S | Spoofing | local service identity | low | accept | Single-operator localhost deployment at ASVS L1; Bearer verification is the server's concern |
| T7-01-R | Repudiation | offline library layer | low | accept | No security-relevant actions taken in-process; Phase 8 job wiring owns audit needs |
| T7-01-E | Elevation of Privilege | package surface | low | accept | Library performs no privileged operations, subprocesses, or filesystem writes outside process memory |
</threat_model>

<verification>
- `python -m pytest tests/test_music_generation.py -q` green (phase quick command from 07-VALIDATION.md)
- `python -m pytest tests/ -q` green (full-suite wave gate)
- Package imports cleanly: `python -c "import sys; sys.path.insert(0,'.'); import src.music_generation"`
- Constraint gates: no database-driver imports in package; no visual-asset terms in package; stdlib-only transport; suite passes with network unavailable
</verification>

<success_criteria>
- All five truths in must_haves hold and are evidenced by named pytest classes
- Golden parameter table matches RESEARCH §3 verbatim (Bedtime 66/F major/3/4/instrumental-intro/120 s pinned)
- Determinism contract provable: same seed ⇒ byte-identical result bytes
- Zero new dependencies in pyproject.toml; zero network syscalls during tests
- Repo-root asset-catalog database byte-identical before/after suite run
</success_criteria>

<output>
Create `.planning/phases/07-music-generation-backend-integration/07-01-SUMMARY.md` when done (record the duration-default discretion values from Task 2 and the _urlopen/_sleep seam names for plan 07-02 consumption).
</output>
