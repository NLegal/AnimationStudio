---
phase: 07-music-generation-backend-integration
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
autonomous: true
requirements: []            # ROADMAP Phase 7 "Requirements: TBD" (no REQUIREMENTS.md mapping) — success criteria sourced goal-backward from ROADMAP Phase 7 goal per planning brief
requirements_addressed: []  # same as above; coverage carried by must_haves
files_modified:
  - src/music_generation/backends.py
  - src/music_generation/__init__.py
  - src/music_generation/ace_step.py
  - src/music_generation/suno.py
  - scripts/generate_phase7.py
  - tests/test_music_generation.py
  - Audio/Music/README.md

user_setup:
  - service: ACE-Step Studio (local, OPTIONAL — manual live smoke only; CI and the pytest suite never require it)
    why: "Single-song live verification of the real adapter outside CI"
    env_vars:
      - name: ACESTEP_API_KEY
        source: "Local ACE-Step Studio deployment configuration (Bearer auth)"
      - name: ACESTEP_BASE_URL
        source: "Default http://localhost:8001 — set only if the service runs elsewhere"
      - name: ACESTEP_OFFLOAD_TO_CPU
        source: "Optional server-side knob documented by ACE-Step; context only, our code never sets it"
    dashboard_config:
      - task: "Start the local ACE-Step 1.5 service before running the live smoke checklist (GPU >= 4 GB VRAM minimum; Turbo XL ~20 GB weights)"
        location: "Host shell, per ACE-Step vendor documentation"

must_haves:
  truths:
    - "get_backend resolves 'ace-step', 'suno', and 'mock'; MUSIC_BACKEND supplies the default when name is omitted; unknown names raise MusicBackendError listing valid choices"
    - "AceStepBackend performs submit→poll→download against {base}/v1/music/generate, {base}/v1/jobs/{id}, and {base}/v1/audio?path=… with Bearer auth from ACESTEP_API_KEY (constructor arg > env), enforcing caption ≤512 / lyrics ≤4096 body constraints"
    - "The locked error map holds end-to-end through the adapter: connection failures → BackendUnavailable, HTTP 401/403 → NotConfigured, failed-status or malformed responses → GenerationFailed"
    - "SunoBackend.is_configured() is always False and every operation raises NotConfigured citing the missing official API; SunoWrapperBackend is flagged experimental and unreachable through get_backend"
    - "scripts/generate_phase7.py --dry-run prints the resolved request JSON with provably zero network I/O and exit code 0"
  artifacts:
    - path: src/music_generation/ace_step.py
      provides: "AceStepBackend (async-job REST adapter over the plan-01 transport seam, health probe included)"
    - path: src/music_generation/suno.py
      provides: "SunoBackend (refusing stub) + SunoWrapperBackend (experimental third-party relay, default-disabled)"
    - path: src/music_generation/backends.py
      provides: "_BACKENDS registry + get_backend(name=None) factory with MUSIC_BACKEND env resolution"
    - path: src/music_generation/__init__.py
      provides: "Completed public exports incl. AceStepBackend, SunoBackend, SunoWrapperBackend, get_backend"
    - path: scripts/generate_phase7.py
      provides: "CLI with --backend/--category/--topic/--seed/--out/--dry-run following generate_phase5.py skeleton conventions"
    - path: Audio/Music/README.md
      provides: "Backend usage docs + manual live-smoke checklist (per 07-VALIDATION.md Manual-Only Verifications)"
    - path: tests/test_music_generation.py
      provides: "TestAceStepAdapter, TestErrorMapping, TestSunoStub, TestPhase7Cli (all offline, fake transports)"
  key_links:
    - "AceStepBackend.submit composes captions via audio_bible.prompts.build_music_prompt and numerics via backends.resolve_music_params (bible stays authoritative — locked RESEARCH §3)"
    - "AceStepBackend(transport=…) defaults to DEFAULT_TRANSPORT from plan 01; fakes replace it wholesale in tests (constraint C3)"
    - "get_backend('mock') is the safe default resolution Phase 8 wiring will inherit via MUSIC_BACKEND"

---

<objective>
Complete `src/music_generation/`: the production ACE-Step 1.5 adapter speaking the LOCKED REST contract against an injectable transport, the refusing Suno stub plus clearly-flagged experimental wrapper, the name→backend registry with `MUSIC_BACKEND` env resolution, the `scripts/generate_phase7.py` CLI whose `--dry-run` proves request assembly without any network call, and the Audio/Music documentation carrying the manual live-smoke checklist.

Purpose: Phase 8 wires these backends into the pipeline, Review UI, and Colab notebook — this phase guarantees the surface it programs against is complete, typed, and offline-verifiable.

Output: 7 files (4 package/script sources, expanded test module, docs), full suite green offline, live smoke documented as a manual checklist.
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-music-generation-backend-integration/07-RESEARCH.md
@.planning/phases/07-music-generation-backend-integration/07-PATTERNS.md
@.planning/phases/07-music-generation-backend-integration/07-VALIDATION.md
@.planning/phases/07-music-generation-backend-integration/COVERAGE.md
@.planning/phases/07-music-generation-backend-integration/07-01-SUMMARY.md
@src/generation_engine/cloud_backend.py
@src/generation_engine/comfy_backend.py
@scripts/generate_phase5.py
@Audio/AUDIO_BIBLE.md
</context>

## Hard Constraints (non-negotiable — inherited from phase brief)

- **C1** — No visual-asset generation anywhere in this phase; no audio generated in CI/tests (the ONLY real-audio moment is the documented MANUAL live smoke on an operator machine).
- **C2** — The repo-root asset-catalog SQLite database is NEVER opened, written, or imported by this phase's code, CLI, or tests; CLI output WAVs go under `Audio/Music/`.
- **C3** — No network I/O in tests — fake transport injection only; even the CLI's dry-run must be provably dial-free.
- **C4** — stdlib `urllib.request` transport only (consumed from plan 01's seam); NO new dependencies.
- **C5** — Env config surface: `ACESTEP_API_KEY`, `ACESTEP_BASE_URL` (default `http://localhost:8001`), `MUSIC_BACKEND`.
- **C6** — RESEARCH.md §2–§5 are LOCKED: REST paths, payload fields, error mapping, model names (`acestep-v15-turbo` | `acestep-v15-sft`), timeouts/polling. Implement verbatim.

## Artifacts this phase produces (complete inventory, both plans)

Package `src/music_generation/` — models: `MusicRequest`, `MusicStatus`, `MusicResult`, `CategoryMusicParams`; protocol: `MusicGenerationBackend` (@runtime_checkable, default `generate(request, *, timeout_s=300.0, poll_interval_s=2.0)`); exceptions: `MusicBackendError`, `NotConfigured`, `BackendUnavailable`, `GenerationFailed`; mapping: `CATEGORY_MUSIC_PARAMS`, `resolve_music_params`, `build_music_request`, `music_negative_prompt`; transport seam: `_post_json`, `_get_json`, `_get_bytes`, `_urlopen`, `_sleep`, `_monotonic`, `DEFAULT_TRANSPORT`; backends: `MockBackend`, `AceStepBackend`, `SunoBackend`, `SunoWrapperBackend` (experimental, default-disabled); discovery: `_BACKENDS`, `get_backend(name=None)`. Script `scripts/generate_phase7.py` with flags `--backend`, `--category`, `--topic`, `--seed`, `--out`, `--dry-run`. Docs `Audio/Music/README.md`. Tests `tests/test_music_generation.py`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 [validation 07-02-01]: Tracer — AceStepBackend end-to-end happy path over scripted fake transport</name>
  <files>src/music_generation/ace_step.py, src/music_generation/backends.py, src/music_generation/__init__.py, tests/test_music_generation.py</files>
  <read_first>
  - .planning/phases/07-music-generation-backend-integration/07-RESEARCH.md §2 — the LOCKED REST contract (paths, payload fields, response shapes, error mapping) and §5 timeouts
  - .planning/phases/07-music-generation-backend-integration/07-PATTERNS.md — ace_step.py assignment (cloud_backend analog lines cited there; comfy_backend health-probe analog) + Deliberate Divergences #2 and #3
  - .planning/phases/07-music-generation-backend-integration/07-01-SUMMARY.md — exact seam names shipped by plan 01 (_urlopen/_sleep/DEFAULT_TRANSPORT) and mapping helpers
  - src/generation_engine/cloud_backend.py lines 61–98 (env-key resolution), 206–227 (Bearer headers + JSON POST shape), 232–248 (poll loop), 320–354 (binary download w/ content-type check) — SHAPES to adapt only
  - src/generation_engine/comfy_backend.py lines 88–102 — health-probe pattern adapted to raise-based degradation
  - src/music_generation/backends.py — protocol, DEFAULT_TRANSPORT, resolve_music_params, exception taxonomy (as built by 07-01)
  </read_first>
  <behavior>
    - Fake transport records calls; after backend.generate(req) exactly one POST hit {base}/v1/music/generate, GETs hit {base}/v1/jobs/{job_id} until completed, then GET {base}/v1/audio?path=<path-from-completed-job>
    - Authorization header equals "Bearer <key>" where key came from constructor arg when provided, else ACESTEP_API_KEY env
    - Request body contains: caption ≤512 chars composed via build_music_prompt (contains category keyword), lyrics ≤4096 chars matching the category scaffold when lyrics_override is None, audio_duration == request.duration_s, bpm/key_scale/time_signature from resolve_music_params(category), integer seed, thinking False, model "acestep-v15-turbo"
    - Scripted sequence pending→running→completed yields MusicStatus states in order; final MusicResult.audio equals scripted bytes; result.backend == "ace-step"; result.seed echoes effective seed
    - Completed-job JSON carrying an audio path key feeds the download URL query parameter verbatim (URL-encoded)
  </behavior>
  <action>
Build the real adapter FIRST against a scripted fake transport, then register it — one vertical slice, production quality.

1. `src/music_generation/ace_step.py` — class AceStepBackend satisfying MusicGenerationBackend by duck-typing:
   - __init__(self, api_key=None, base_url=None, model=None, transport=None): credential/base resolution order constructor arg > env per C5 (mirror cloud_backend.py lines 84–98 style); base_url falls back to the C5 default literal; model validated against {"acestep-v15-turbo","acestep-v15-sft"} defaulting to the turbo name; transport defaults to DEFAULT_TRANSPORT imported from backends.py. Store a small call-recording-friendly reference — adapters call self._transport.post_json/get_json/get_bytes.
   - is_configured(): True iff an API key resolves AND a short-timeout (5 s connect budget per RESEARCH §5) health probe succeeds — probe GET {base}/v1/jobs/health first, falling back to a root GET if that 404s; ANY connection-level failure returns False (raise-based degradation per PATTERNS comfy adaptation — never just a log line).
   - submit(request) -> str: raises NotConfigured immediately when no key resolves; builds the JSON payload EXACTLY per RESEARCH §2 field list — prompt/caption via src.audio_bible.prompts.build_music_prompt(request.category, request.topic, vocals=request.vocals) truncated hard at 512 chars; lyrics = request.lyrics_override or the "\n"-joined marker scaffold from resolve_music_params(request.category).lyric_structure, truncated at 4096 chars; audio_duration=request.duration_s; bpm/key_scale/time_signature from resolve_music_params; seed=int(request.seed or 0); thinking=False; model=self.model. POST {base_url}/v1/music/generate with headers Authorization Bearer <key> + Content-Type application/json. Response must decode to a dict containing non-empty "job_id" else GenerationFailed (malformed-response rule). Return job_id.
   - poll(job_id) -> MusicStatus: GET {base_url}/v1/jobs/{job_id} (auth headers included); map the status string straight into MusicStatus.state (pending|running|completed|failed); pass progress through when present (default 0.0 pending, 1.0 terminal-success); failed carries the server error text into .error. When completed, remember the returned audio path/URL (accept common keys: "audio_path", "path", "output") in an instance dict keyed by job_id for download().
   - download(job_id) -> bytes: look up the remembered path (if absent because poll never ran, perform one poll() first); percent-encode the path into GET {base_url}/v1/audio?path=<encoded>; return raw bytes via _get_bytes-equivalent transport call; log-and-continue (warning only) if content-type is not audio/* — mirroring the cloud download validation adapted to audio.
   - generate(): NO override — the protocol default loop from plan 01 drives submit/poll/download with backoff and deadline enforcement.
   - Class docstring documents: external local service we never start/install; all network flows go through the injected transport; error map summary.

2. Registry + exports: in backends.py add private `_BACKENDS: dict[str, callable]` mapping "ace-step"/"suno"/"mock" to LAZY import hooks (import adapter modules inside get_backend to avoid circular imports), and `get_backend(name=None, **kwargs)` factory: None resolves os.environ.get("MUSIC_BACKEND", "mock"); unknown name raises MusicBackendError listing the three valid choices; returns a constructed INSTANCE (ace-step → AceStepBackend(**kwargs), suno → SunoBackend(), mock → MockBackend(**kwargs)). This task implements the ace-step and mock entries; suno lands in Task 2 (stub file doesn't exist yet — wire its hook defensively so get_backend("suno") works once Task 2 lands, but tests for it belong to Task 2). Update __init__.py exports/__all__ adding AceStepBackend and get_backend now; SunoBackend/SunoWrapperBackend join in Task 2.

3. Tests — class TestAceStepAdapter in tests/test_music_generation.py (offline only):
   - A tiny recording fake transport (SimpleNamespace with post_json/get_json/get_bytes closures appending dataclass Call(url, method, headers, payload/timeout) records and returning scripted responses) — reuse this fixture class across Task 2's error tests.
   - All <behavior> bullets asserted, including header-value equality for Bearer and the three distinct URLs hit.
   - Env-isolation: monkeypatch.delenv("ACESTEP_API_KEY", raising=False) in fixtures that assert unconfigured behavior (precedent: tests/test_generation_engine.py lines 86–92).

Commit after green (atomic tracer commit).
  </action>
  <verify>
    <automated>python -m pytest tests/test_music_generation.py::TestAceStepAdapter -x -q && python -m pytest tests/test_music_generation.py -q</automated>
  </verify>
  <acceptance_criteria>
  - `python -m pytest tests/test_music_generation.py::TestAceStepAdapter -x -q` exits 0
  - Recording fake proves exactly: one POST to {base}/v1/music/generate; ≥2 GETs to {base}/v1/jobs/{id} for a two-step completion script; one GET to {base}/v1/audio?path=<encoded>
  - Bearer header value asserted equal to the injected/env key; body constraints asserted (caption ≤512, lyrics ≤4096, bpm/key/time-signature golden values for the tested category, thinking False, model turbo)
  - `python -c "import sys; sys.path.insert(0,'.'); from src.music_generation import get_backend, AceStepBackend; b=get_backend('ace-step', api_key='k'); assert isinstance(b, AceStepBackend)"` succeeds
  - `! rg -q "import requests" src/music_generation/` (constraint C4)
  </acceptance_criteria>
  <reversibility rating="reversible">New adapter behind an existing protocol; swapping REST details later touches one file.</reversibility>
  <done>The single real-backend path — build_request → submit → poll(pending→running→completed) → download bytes — works end-to-end against a scripted fake transport with the exact locked URLs, headers, and payload fields, and the backend is reachable via get_backend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 [validation 07-02-02 + 07-02-03]: Failure surface — error mapping matrix, Suno refusal stub, experimental wrapper, registry env config</name>
  <files>src/music_generation/suno.py, src/music_generation/ace_step.py, src/music_generation/backends.py, src/music_generation/__init__.py, tests/test_music_generation.py</files>
  <read_first>
  - .planning/phases/07-music-generation-backend-integration/07-RESEARCH.md §2 error-mapping block and §4 Suno-stub bullet (message text is LOCKED verbatim) + registry bullet
  - .planning/phases/07-music-generation-backend-integration/07-PATTERNS.md — suno.py assignment (cloud_backend not-ready path lines 119–130; divergence: RAISE, don't degrade silently)
  - .planning/phases/07-music-generation-backend-integration/COVERAGE.md — Surface 2 opt-out rationale (wrapper default-disabled decision)
  - src/pipeline/job_queue.py lines 92–113 — domain-error message style including current + allowed values (echo for GenerationFailed payloads)
  - tests/test_generation_engine.py lines 86–92 — env-clearing isolation precedent for ACESTEP_* variables
  </read_first>
  <behavior>
    - URLError raised by fake transport on submit → BackendUnavailable propagates from backend.submit/generate
    - HTTPError 401 → NotConfigured; HTTPError 403 → NotConfigured
    - Poll response with status "failed" and error text → GenerationFailed whose message includes that text
    - Malformed submit response (non-JSON body; JSON missing job_id) → GenerationFailed
    - Connection drop mid-poll (after successful submit) → BackendUnavailable
    - Deadline expiry on a never-completing job → GenerationFailed naming job id and elapsed time
    - SunoBackend: is_configured() False; submit/poll/download/generate each raise NotConfigured with the locked message citing no official public API as of Aug 2026 and pointing at .planning/research/MUSIC-GENERATION.md
    - SunoWrapperBackend: marked experimental in class docstring + attribute; same refusal behavior with an added "(experimental third-party relay — disabled by default)" suffix; NOT resolvable via get_backend
    - get_backend(None) honors MUSIC_BACKEND ("mock" fallback); get_backend("nope") raises MusicBackendError listing valid names
    - No exception message or log record anywhere in the matrix ever contains the bearer token value
  </behavior>
  <action>
Complete the typed-failure surface: full error-map coverage through the REAL adapter path, then the Suno pair, then registry environment resolution.

1. `src/music_generation/suno.py` — two classes, minimal and strict:
   - SunoBackend: is_configured() -> False unconditionally; submit/poll/download each raise NotConfigured with the EXACT locked message: "Suno has no official public API (as of Aug 2026); see .planning/research/MUSIC-GENERATION.md"; generate() refuses identically WITHOUT constructing requests (it may delegate: try submit-path refusal first — simplest is to raise directly in generate too).
   - SunoWrapperBackend(SunoBackend): class attribute EXPERIMENTAL = True + docstring paragraph flagging it as a third-party relay integration point for when a wrapper API emerges; overrides the refusal message appending "(experimental third-party relay — disabled by default)". It is deliberately ABSENT from the _BACKENDS registry (assumption-delta invariant from plan 07-01: registry resolution can never yield the wrapper).
   - No HTTP code whatsoever in this module (constraint C3/C4 — nothing to connect to).

2. Error-map hardening in ace_step.py where adapter-specific gaps exist: ensure the mid-flight poll failure path and deadline path produce BackendUnavailable / GenerationFailed respectively (most of this already flows from plan 01's seam mapping — add only what the new <behavior> rows expose, e.g. wrapping non-dict poll bodies as malformed).

3. Registry env config in backends.py: finalize get_backend's MUSIC_BACKEND handling (None-name path) + the unknown-name MusicBackendError message; confirm the suno hook registered in Task 1 now imports cleanly.

4. __init__.py: add SunoBackend, SunoWrapperBackend to exports/__all__.

5. Tests — two new classes (offline, reusing Task 1's recording fake):
   - TestErrorMapping: one test per <behavior> row using scripted fake transports (URLError instance, HTTPError with codes 401/403, failed-status dict, malformed strings/dicts, drop-after-submit script, never-completing script with patched monotonic clock). Include test_token_never_leaks: run the full matrix while capturing logs (pytest caplog) and assert the dummy key substring appears in NO raised-message repr and NO log output.
   - TestSunoStub: refusal messages (assert the locked citation substring), constant is_configured False for both classes, wrapper EXPERIMENTAL flag truthy, and the registry invariant: get_backend("suno") returns a SunoBackend (never the wrapper); "sunowrapper"/wrapper-class names are invalid registry keys raising MusicBackendError.
   - Env hygiene: every test that constructs AceStepBackend without an explicit key clears ACESTEP_* vars via monkeypatch first.

Commit after green.
  </action>
  <verify>
    <automated>python -m pytest tests/test_music_generation.py::TestErrorMapping tests/test_music_generation.py::TestSunoStub -q && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - `python -m pytest tests/test_music_generation.py::TestErrorMapping tests/test_music_generation.py::TestSunoStub -q` exits 0
  - Full matrix green: connection→BackendUnavailable; 401 and 403→NotConfigured; failed-status and both malformed variants→GenerationFailed; mid-flight drop→BackendUnavailable; deadline→GenerationFailed with job id + elapsed in message
  - Locked refusal message asserted verbatim-substring for SunoBackend from all four entry points (is_configured excluded — it returns False silently)
  - Wrapper unreachable via registry asserted; MUSIC_BACKEND override + unknown-name error asserted
  - Token-leak test green under caplog capture (secure behavior from 07-VALIDATION.md row 07-02-01/02)
  - `python -m pytest tests/ -q` fully green (wave-gate sampling)
  - `! rg -q "sqlite3" src/music_generation/ scripts/generate_phase7.py` (constraint C2)
  </acceptance_criteria>
  <done>Every failure mode in the locked map surfaces as the correct typed exception through the real adapter, Suno refuses loudly and safely, and backend selection is env-driven with a safe mock default.</done>
</task>

<task type="auto">
  <name>Task 3 [validation 07-02-04]: CLI scripts/generate_phase7.py + docs with manual live-smoke checklist</name>
  <files>scripts/generate_phase7.py, tests/test_music_generation.py, Audio/Music/README.md</files>
  <read_first>
  - .planning/phases/07-music-generation-backend-integration/07-RESEARCH.md §5 (env/config) + Validation Architecture table (live-service smoke is the ONLY manual row) 
  - .planning/phases/07-music-generation-backend-integration/07-PATTERNS.md — scripts/generate_phase5.py assignment (bootstrap, argparse, report/summary, exit-code contract lines cited there)
  - scripts/generate_phase5.py lines 1–18, 40–50, 176–192 — skeleton to mirror exactly (shebang, phase docstring WITH Reproduction block, sys.path bootstrap, argparse help text, boolean exit code, __main__ guard)
  - .planning/phases/07-music-generation-backend-integration/07-VALIDATION.md — Manual-Only Verifications row (exact smoke command wording to document)
  - Audio/AUDIO_BIBLE.md — house documentation style for Audio/ assets (README.md joins this family)
  - src/music_generation/__init__.py — the public surface the CLI consumes (build_music_request, get_backend, exceptions)
  </read_first>
  <behavior>
    - main(["--dry-run", "--category", "Bedtime", "--topic", "sleepy moon"]) prints parseable JSON containing category "Bedtime", duration_s 120, topic, seed, vocals — and performs ZERO transport calls (a fail-loud guard installed on all three seam functions never trips); exit code 0
    - Dry-run succeeds with ACESTEP_API_KEY unset (no credential needed to assemble a request)
    - main(["--dry-run", "--category", "Spaceship"]) still exits 0 (generic fallback params resolve)
    - Real mode against the mock backend writes a .wav file under the --out directory named <category>-<topic-slug>-<seed>.wav, prints a summary block (backend, job id, byte count, output path), exit 0
    - Real mode with --backend suno exits 1 printing the refusal reason on stderr (typed exception caught, no traceback spill)
    - Unknown --backend value exits nonzero with the valid-choice list
  </behavior>
  <action>
Mirror the generate_phase5.py skeleton precisely (PATTERNS calls it an exact structural match) and add the dry-run semantics; then write the documentation that carries the manual smoke procedure.

1. `scripts/generate_phase7.py`:
   - Header: shebang; docstring opening "Phase 7 — Music Generation Backend Integration …" with a Reproduction block showing BOTH the offline dry-run command and the live single-song smoke command from 07-VALIDATION.md; sys.path bootstrap inserting the repo root exactly like generate_phase5.py lines 12–14; imports limited to stdlib argparse/os/sys + src.music_generation public names (+ json for the dry-run dump).
   - argparse: --backend (default from MUSIC_BACKEND env falling back to "mock"; help lists ace-step/suno/mock), --category (default "Bedtime"), --topic (default ""), --seed (int, optional), --out (default "Audio/Music"), --dry-run (store_true). Help text style copied from generate_phase5.py lines 40–50.
   - Dry-run branch: resolve build_music_request(args.category, args.topic, seed=args.seed); print json.dumps(request.model_dump(), indent=2) to stdout; print nothing else required; return 0. ABSOLUTELY no backend construction, no transport touch (constraint C3 — this is the property TestPhase7Cli enforces with a fail-loud guard).
   - Real-mode branch: get_backend(args.backend); call backend.generate(build_music_request(...)); mkdir -p behavior for --out; write bytes to <category lowercased>-<topic slugified or "song">-<effective seed>.<format>; print a short summary (backend, job_id, byte count, path) mirroring the report-line style of generate_phase5.py; catch MusicBackendError subclasses → message to stderr, return 1. Output goes ONLY under --out (constraint C2 — no database contact anywhere).
   - Exit-code contract: 0 success / 1 handled failure, wired through sys.exit(main()) exactly like the analog.

2. Tests — class TestPhase7Cli in tests/test_music_generation.py: invoke main([...]) in-process with monkeypatched argv-free list args; capsys for stdout/stderr; tmp_path for --out; install a fail-loud triple-guard (replace the three transport functions AND _urlopen with raisers) around the dry-run cases; clear ACESTEP_* env in setUp-style fixtures; cover every <behavior> row.

3. `Audio/Music/README.md` (new — joins the Audio/AUDIO_BIBLE.md documentation family; root README.md intentionally untouched since it has no music section today and all music documentation lives under Audio/ per repo convention — record this scoping call in the SUMMARY):
   - What the package is (provider-agnostic layer: ace-step / suno / mock) + one-command quickstart for each mode.
   - Environment table per constraint C5 with sources/defaults; backend-selection precedence (flag > MUSIC_BACKEND > mock).
   - Error taxonomy table (NotConfigured / BackendUnavailable / GenerationFailed) with operator-facing meaning and first fixes.
   - Determinism note: seed controls reproducibility; mock is always deterministic offline.
   - **Live Smoke Checklist (manual-only)** — transcribe the 07-VALIDATION.md manual row verbatim as numbered steps: start local ACE-Step 1.5 (GPU ≥ 4 GB VRAM minimum; Turbo XL ~20 GB weights; optional CPU-offload env knob), export the API key, run `python scripts/generate_phase7.py --backend ace-step --category Bedtime --topic "sleepy moon"` , confirm a completed job in logs and an audible WAV under Audio/Music/, then delete-or-keep the artifact per operator preference. State explicitly that CI and the pytest suite NEVER require the service.

Run the FULL suite (`python -m pytest tests/ -q`) as the closing wave-gate check. Commit after green.
  </action>
  <verify>
    <automated>python -m pytest tests/test_music_generation.py::TestPhase7Cli -q && python -m pytest tests/ -q && python scripts/generate_phase7.py --dry-run --category Bedtime --topic "sleepy moon"</automated>
  </verify>
  <acceptance_criteria>
  - `python -m pytest tests/test_music_generation.py::TestPhase7Cli -q` exits 0
  - Direct invocation `python scripts/generate_phase7.py --dry-run --category Bedtime --topic "sleepy moon"` exits 0 and stdout parses as JSON with duration_s == 120
  - Dry-run proven dial-free: fail-loud guards on all transport seams never trip during TestPhase7Cli dry-run cases (secure behavior from 07-VALIDATION.md row 07-02-04)
  - Mock real-mode run creates a playable-header .wav under tmp out dir with exit 0; suno real-mode exits 1 with refusal text on stderr and no traceback
  - `Audio/Music/README.md` exists and contains: env table for all three variables, the error taxonomy, and the numbered live-smoke checklist including the exact VALIDATION smoke command
  - `python -m pytest tests/ -q` fully green — phase complete signal for /gsd-execute-phase
  - `! rg -qi "\bimage" src/music_generation/ scripts/generate_phase7.py` (constraint C1)
  </acceptance_criteria>
  <done>The CLI assembles and (in dry-run) prints bible-conformant requests with provably zero network use; real mode is exercised against the mock offline; operators have a written path to the one manual live check; docs land under Audio/Music/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process ↔ local ACE-Step HTTP service | Server responses (JSON bodies, audio bytes, headers) are untrusted input crossing into parsing/download logic |
| environment ↔ process | ACESTEP_API_KEY (secret), ACESTEP_BASE_URL, MUSIC_BACKEND enter as operator-controlled strings |
| CLI ↔ operator | Arguments and printed reports must never leak credentials |
| tests ↔ world | Zero-network guarantee enforced structurally (fake transports + fail-loud guards) |

## STRIDE Threat Register (ASVS L1; block on unresolved high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T7-02-I | Info Disclosure | AceStepBackend auth header handling; CLI reporting | high | mitigate | Key read from env/constructor only; header assembled at call time; TestErrorMapping::test_token_never_leaks asserts the secret appears in no exception repr and no captured log record; dry-run dumps the request model which contains no credentials |
| T7-02-T | Tampering | response parsing in ace_step.py | medium | mitigate | Strict stdlib JSON decoding; missing job_id / non-dict bodies / unknown status strings → typed GenerationFailed; audio bytes accepted as opaque binary with content-type warning only |
| T7-02-D | Denial of Service | adapter polling vs slow/hung local service | medium | mitigate | Inherits plan-01 caps: overall timeout_s=300 deadline, exponential backoff capped ×8, (5,30) per-call timeouts; deadline breach raises GenerationFailed with job id |
| T7-02-S | Spoofing | rogue service at configured base URL | low | accept | ASVS L1 single-operator localhost tool; base URL is operator-set env; Bearer secret never sent anywhere except the configured service |
| T7-02-E | Elevation of Privilege | CLI execution path | low | accept | Script performs file writes only under operator-supplied --out; no subprocess, no privileged ops, no eval of server data |
| T7-02-R | Repudiation | CLI runs | low | accept | Single-operator local tool; summary lines to stdout satisfy traceability at L1; audit trails are Phase 8 batch-ops scope |
| T7-02-SC | Tampering (supply chain) | dependency set of new modules + CLI | high | mitigate | Constraint C4 grep-gated: stdlib transport consumed from plan-01 seam; zero packages installed this phase (no npm/pip/cargo installs occur, so the Package Legitimacy Gate has nothing to audit) |
| T7-02-C | Database integrity | whole phase vs repo-root asset-catalog DB | high | mitigate | Constraint C2 grep-gated across src/, scripts/, and tests: no DB driver imports; CLI writes only under --out; persistence remains Phase 8 scope |
</threat_model>

<verification>
- `python -m pytest tests/test_music_generation.py -q` green (phase quick command)
- `python -m pytest tests/ -q` green (full-suite wave gate; ~120 s ceiling)
- `python scripts/generate_phase7.py --dry-run --category Bedtime --topic "sleepy moon"` exits 0 with JSON on stdout, no network possible (guards)
- Constraint gates: stdlib-only imports; no DB-driver imports in src/scripts/tests additions; no visual-asset terms in package/script; suite passes with interfaces down
- Live smoke remains EXPLICITLY manual: checklist documented in Audio/Music/README.md, never executed in CI
</verification>

<success_criteria>
- All five truths in must_haves hold, evidenced by TestAceStepAdapter, TestErrorMapping, TestSunoStub, TestPhase7Cli
- REST contract byte-for-byte per RESEARCH §2 (three URLs, Bearer scheme, seven payload fields, model names)
- Error taxonomy total: every failure mode maps to exactly one typed exception; none leak secrets
- Registry resolves all three names with env-driven default; wrapper unreachable via registry (assumption-delta invariant upheld)
- Docs carry the manual smoke checklist; pyproject.toml untouched (zero new dependencies)
- Repo-root asset-catalog database byte-identical before/after the entire suite
</success_criteria>

<output>
Create `.planning/phases/07-music-generation-backend-integration/07-02-SUMMARY.md` when done (note the root-README scoping call, the wrapper-registry invariant test, and the exact smoke command verified syntactically).
</output>
