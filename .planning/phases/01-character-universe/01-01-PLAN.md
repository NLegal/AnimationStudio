---
phase: 01-character-universe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - src/__init__.py
  - src/models/__init__.py
  - src/models/schemas.py
  - src/asset_repository/__init__.py
  - src/asset_repository/interfaces.py
  - src/generation_engine/__init__.py
  - src/generation_engine/base.py
  - src/identity_engine/__init__.py
  - src/identity_engine/scorer.py
  - src/identity_engine/brand_score.py
  - src/prompt_builder/__init__.py
  - src/prompt_builder/templates.py
  - src/prompt_builder/negative.py
  - src/prompt_builder/builder.py
  - src/pipeline/__init__.py
  - tests/conftest.py
  - tests/test_asset_repository.py
  - Universe/.gitkeep
autonomous: true
requirements:
  - CHAR-01
  - CHAR-06
  - CHAR-08
user_setup:
  - service: Python venv
    why: "All Python code runs in a virtual environment"
    steps:
      - "python3 -m venv .venv"
      - "source .venv/bin/activate"
      - "pip install --upgrade pip"
      - "pip install -e '.[dev]'"

must_haves:
  truths:
    - A project structure with pyproject.toml, src/ layout, and venv can be installed
    - A SQLite database can be created and queried for character records
    - An asset can be created, stored, and retrieved through the repository pattern
    - A generation backend ABC exists and can be instantiated
    - An identity scorer can compute a Brand Score for a test image
    - A prompt can be composed from templates with character-specific data
    - Tests can run with pytest and pass on the tracer flow
  artifacts:
    - pyproject.toml
    - src/models/schemas.py
    - src/asset_repository/interfaces.py
    - src/asset_repository/sqlite_repo.py
    - src/generation_engine/base.py
    - src/identity_engine/scorer.py
    - src/identity_engine/brand_score.py
    - src/prompt_builder/templates.py
    - src/prompt_builder/negative.py
    - src/prompt_builder/builder.py
    - tests/conftest.py
    - tests/test_asset_repository.py
  key_links:
    - AssetRepository ABC → SQLiteAssetRepository (the foundation for all data persistence)
    - GenerationBackend ABC → MockBackend in tests (proves adapter pattern works)
    - IdentityScorer → BrandScore → test image (proves scoring pipeline works)
---

<objective>
Build the foundational character creation pipeline skeleton: project structure, shared data models, asset repository, generation engine ABC, identity scoring engine, and prompt builder. Wire an end-to-end tracer that creates a character record, generates a reference image (via mock backend), scores it through the identity engine, and commits the asset to the repository.

**Purpose:** Establish the project infrastructure, architectural patterns (adapter ABCs, repository pattern, plugin-based scoring), and prove the end-to-end flow works before expanding each subsystem in subsequent plans.

**Output:** Runnable Python package `character-studio` with models, asset repository (SQLite), generation engine ABC, identity scorer, and prompt builder, backed by pytest test infrastructure.
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-character-universe/01-CONTEXT.md
@.planning/phases/01-character-universe/01-RESEARCH.md
@.planning/phases/01-character-universe/01-PATTERNS.md
@PHASE1.md
</context>

<flagged_assumptions>
The following edge-behavior assumptions are unresolved (SPEC-absent greenfield probe). They are flagged here and must be consciously decided during implementation or review:

| Probe | Category | Requirement | Flag |
|-------|----------|-------------|------|
| What makes two characters the same or different? | identity | CHAR-01 | Assumed: character identity is determined by unique `id` (UUID), not by name or appearance similarity |
| Are personality traits a closed enum or open-ended tags? | encoding | CHAR-06 | Assumed: open-ended string tags (not enum) for future flexibility per D-06 |
| What constitutes the same catchphrase when a character has multiple delivery variants? | identity | CHAR-06 | Assumed: catchphrase identity is by `{text}` — delivery variants are metadata on the catchphrase record |
| What is the minimum valid character profile? | empty | CHAR-06 | Assumed: name + species + category are required; all other fields are optional with defaults |
| Are expressions identified by name, index, or visual hash? | encoding | CHAR-03 | Assumed: identified by string name (e.g. "happy", "sad") matching PHASE1.md expression list |
| When is an expression considered "the same" across different outfits or poses? | identity | CHAR-03 | Assumed: expression name is invariant — an expression is "the same" by name regardless of outfit/pose context |
| When two expressions are near-identical (e.g. happy vs very happy), do they merge or stay distinct? | adjacency | CHAR-03 | Assumed: each named expression is a distinct asset; no automatic merging of near-identical expressions |
| When two things are exactly equal or just touch, do they merge, collide, or separate? | adjacency | CHAR-01/02/03/04/05/06 | Assumed: exact equality is a no-op duplicate; "just touching" remains distinct per existing naming |
| Whose definition of length/equality applies — bytes, code points, grapheme clusters? | encoding | CHAR-01 | Assumed: Python string equality (Unicode code-point comparison) for all string comparisons |
| What is the result for empty, single-element, or null input on any transform? | empty | CHAR-01/02/03/04/05 | Assumed: empty input returns empty result/generates no assets; null raises ValidationError |
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Tracer: End-to-end character creation flow through all pipeline layers</name>
  <files>
    src/models/schemas.py,
    src/asset_repository/interfaces.py,
    src/asset_repository/sqlite_repo.py,
    src/generation_engine/base.py,
    src/identity_engine/scorer.py,
    src/identity_engine/brand_score.py
  </files>
  <action>
    Create the minimal end-to-end character creation flow that touches every layer of the pipeline. This is the thinnest vertical slice — one path, real code, production-quality.

    **Layer 1 — Shared Data Models** (`src/models/schemas.py`):
    Pydantic v2 models for all pipeline contracts:
    - `CharacterModel`: id (str, UUID), name (str), category (Literal['main','family','friend','community','fantasy']), species (str), bio_data (dict), created_at (datetime), locked_at (Optional[datetime])
    - `AssetModel`: id (str, UUID), character_id (str FK), asset_type (Literal['reference','expression','pose','outfit']), variant (Optional[str]), state (str, default 'draft') per D-15 lifecycle (draft→generated→scored→shortlisted→approved→production→archived), file_path (str), prompt (Optional[str]), seed (Optional[int]), model_id (Optional[str]), scores (Optional[dict]), brand_score (Optional[float]), created_at (datetime), approved_at (Optional[datetime])
    - `GenerationJobRequest`: character_id, job_type (Literal['reference','expression','pose','outfit']), prompt (str), negative_prompt (str, default ''), seed (Optional[int]), count (int, ge=1, le=100, default=4), model_backend (str, default='flux')
    - `ScoringResult`: asset_id (str), scores (dict[str, float]), brand_score (dict), scored_at (datetime)
    Use `from pydantic import BaseModel, Field` with `Field(default_factory=datetime.now)` for timestamps. Apply UUID generation with `import uuid; id: str = Field(default_factory=lambda: str(uuid.uuid4()))`.

    **Layer 2 — Asset Repository** 
    - `src/asset_repository/interfaces.py`: Define `AssetRepository` ABC with async methods: `save(record: AssetModel) -> str`, `get(asset_id: str) -> Optional[AssetModel]`, `update_state(asset_id: str, new_state: str) -> None`, `find_by_character(character_id: str, asset_type: Optional[str] = None) -> list[AssetModel]`, `find_approved(character_id: str, asset_type: str) -> list[AssetModel]`. Also define `CharacterRepository` ABC: `save_character(char: CharacterModel) -> str`, `get_character(character_id: str) -> Optional[CharacterModel]`, `find_character_by_name(name: str) -> Optional[CharacterModel]`, `list_characters() -> list[CharacterModel]`.
    - `src/asset_repository/sqlite_repo.py`: Implement both interfaces using Python `sqlite3` with parameterized queries (`?` placeholders, never f-strings per security domain). Schema tables: `characters` (id TEXT PK, name TEXT NOT NULL, category TEXT NOT NULL, species TEXT, bio_data TEXT as JSON, created_at TEXT, locked_at TEXT), `assets` (id TEXT PK, character_id TEXT NOT NULL FK, asset_type TEXT NOT NULL, variant TEXT, state TEXT DEFAULT 'draft', file_path TEXT NOT NULL, prompt TEXT, seed INTEGER, model_id TEXT, scores TEXT as JSON, brand_score REAL, created_at TEXT, approved_at TEXT). Set `PRAGMA journal_mode=WAL` and `PRAGMA foreign_keys=ON` on connect. Use `json.dumps()`/`json.loads()` for scores/bio_data fields. Support `:memory:` for testing. Raise custom `NotFoundError` when get/update targets don't exist.

    **Layer 3 — Generation Engine** (`src/generation_engine/base.py`):
    - `GenerationInput` dataclass: prompt (str), negative_prompt (str), seed (int), width (int=1024), height (int=1024), num_images (int=1)
    - `GenerationOutput` dataclass: images (list[Image.Image]), seed (int), metadata (dict)
    - `GenerationBackend` ABC: `load_model(model_path: str) -> None`, `generate(input: GenerationInput) -> GenerationOutput`
    From `PIL import Image` and `from abc import ABC, abstractmethod`. Use `from dataclasses import dataclass` for input/output.

    **Layer 4 — Identity Engine** 
    - `src/identity_engine/scorer.py`: Define `ScoringPlugin` protocol (name: str, weight: float, `score(image: Image.Image, reference: Optional[Image.Image] = None, **kwargs) -> float`). `IdentityScorer` class with `__init__(plugins: Optional[list[ScoringPlugin]] = None)` that defaults to a single `MockScoringPlugin` returning random scores between 0.5-1.0 (for testing without real ml models). Method `score_all(image, **kwargs) -> dict[str, float]` and method `brand_score(image, **kwargs) -> dict` that calls BrandScore.compute(). Include a `MockBackend` class implementing `GenerationBackend` returning a solid-color test image — enables testing without GPU. `from PIL import Image, ImageDraw`.
    - `src/identity_engine/brand_score.py`: `BrandScore` class with `WEIGHTS` dict per D-05: prompt_accuracy=0.20, character_consistency=0.20, technical_quality=0.15, facial_appeal=0.15, child_friendliness=0.10, color_harmony=0.10, silhouette_recognizability=0.05, style_consistency=0.05. `compute(scores: dict[str, float]) -> dict` returns `{"total": float, "max": 1.0, "components": {key: {"raw": float, "weighted": float, "weight": float}}}`.

    **Layer 5 — Prompt Builder** (`src/prompt_builder/`):
    - `src/prompt_builder/negative.py`: `COMMON_NEGATIVE` string constant per PHASE1.md negative prompt: "low quality, blurry, deformed, mutated, duplicate, extra arms, extra legs, extra fingers, missing fingers, cross eyed, cropped, watermark, text, logo, dark, scary, horror, realistic skin, adult, violence, blood, ugly, noise". `build_negative_prompt(*, custom: str = "") -> str` composing common + style negatives.
    - `src/prompt_builder/templates.py`: `CharacterPrompt` dataclass with name, species, appearance, outfit, style (default="Pixar-quality, Cocomelon-inspired, bright colorful nursery world"). `PromptTemplates` static class with `reference_sheet(character, angle)`, `expression(character, expression)`, `pose(character, pose)`, `outfit(character, outfit)` methods returning composed prompt strings per the PHASE1.md template format.
    - `src/prompt_builder/builder.py`: `PromptBuilder` class that takes `PromptTemplates` and `negative_prompt` function. Method `build(character, asset_type, variant=None, **kwargs) -> tuple[str, str]` returning (positive_prompt, negative_prompt).

    **Wiring (in test or inline on `__main__`):** After all layers exist, the task wires the tracer: create Lily Bunny CharacterModel → save to SQLite → build reference sheet prompt via PromptBuilder → call MockBackend.generate() → score result via IdentityScorer → save scored asset → update state to 'scored'. This proves every layer connects.
  </action>
  <verify>
    <automated>python3 -c "
from src.models.schemas import CharacterModel, AssetModel
from src.asset_repository.sqlite_repo import SQLiteCharacterRepository, SQLiteAssetRepository
from src.generation_engine.base import GenerationBackend
from src.identity_engine.scorer import IdentityScorer, MockScorerPlugin
from src.identity_engine.brand_score import BrandScore
from src.prompt_builder.builder import PromptBuilder
from src.prompt_builder.templates import CharacterPrompt, PromptTemplates
from src.prompt_builder.negative import build_negative_prompt
import asyncio

# 1. Create character
char = CharacterModel(name='Lily Bunny', category='main', species='rabbit', bio_data={'personality': ['curious','kind']})
repo = SQLiteCharacterRepository(':memory:')
asyncio.run(repo.save_character(char))
loaded = asyncio.run(repo.get_character(char.id))
assert loaded.name == 'Lily Bunny'

# 2. Generate reference sheet prompt
prompt = CharacterPrompt(name='Lily Bunny', species='rabbit', appearance='soft white fur, large green eyes', outfit='pink dress with blue bow')
pos, neg = PromptBuilder().build(prompt, asset_type='reference', angle='front')
assert len(pos) > 20 and len(neg) > 20

# 3. Mock generate + score
from PIL import Image
scorer = IdentityScorer(plugins=[MockScorerPlugin(weight=1.0)])
img = Image.new('RGB', (1024, 1024), color=(255, 192, 203))
scores = scorer.score_all(img)
assert all(k in scores for k in BrandScore.WEIGHTS)
bs = scorer.brand_score(img)
assert 0 <= bs['total'] <= 1.0

# 4. Save asset
arepo = SQLiteAssetRepository(':memory:')
asset = AssetModel(character_id=char.id, asset_type='reference', file_path='/tmp/test.png', scores=scores, brand_score=bs['total'])
asyncio.run(arepo.save(asset))
assert asyncio.run(arepo.get(asset.id)) is not None
print('TRACER PASS: All layers connected')
"</automated>
  </verify>
  <done>
    - All shared Pydantic models defined and importable
    - AssetRepository and CharacterRepository ABCs defined
    - SQLiteAssetRepository and SQLiteCharacterRepository implement full CRUD with parameterized queries
    - GenerationBackend ABC defined with GenerationInput/Output dataclasses
    - IdentityScorer with MockScorerPlugin and BrandScore composite work
    - Prompt templates, negative prompt standards, and PromptBuilder work
    - End-to-end tracer script runs successfully: create char → prompt → mock gen → score → save
    - All layers use adapter interfaces (no concrete coupling)
  </done>
</task>

<task type="auto">
  <name>Project setup, package scaffolding, and Universe skeleton</name>
  <files>
    pyproject.toml,
    .env.example,
    src/__init__.py,
    src/models/__init__.py,
    src/asset_repository/__init__.py,
    src/generation_engine/__init__.py,
    src/identity_engine/__init__.py,
    src/prompt_builder/__init__.py,
    src/pipeline/__init__.py,
    tests/conftest.py,
    Universe/.gitkeep
  </files>
  <action>
    Create the project skeleton and tooling around the tracer files from Task 1.

    **`pyproject.toml`:**
    Build system: setuptools. Project name `character-studio`, version `0.1.0`. Python >=3.11.
    Dependencies (per RESEARCH.md Standard Stack): diffusers>=0.39.0, torch>=2.13.0, transformers>=5.14.0, Pillow>=11.0.0, numpy>=2.0.0, scikit-learn>=1.6.0, fastapi>=0.115.0, uvicorn[standard], jinja2>=3.1.0, aiosqlite>=0.20.0, pydantic>=2.0.0, timm>=1.0.0.
    Optional: `[dev]` with pytest>=8.0.0, pytest-asyncio>=0.24.0; `[scoring]` with simple-aesthetics-predictor, opencv-python; `[comfyui]` with pycomfyapi.
    `[tool.pytest.ini_options]`: asyncio_mode="auto", testpaths=["tests"], timeout=30.
    Package discovery: `[tool.setuptools.packages.find]` where={"where":["src"]}.

    **`.env.example`:**
    Document tokens needed for cloud backends (when GPU is configured):
    ```
    # Cloud API Keys (optional — for cloud generation when no GPU)
    FAL_API_KEY=
    REPLICATE_API_KEY=
    BFL_API_KEY=
    # Local GPU (set when running on GPU-equipped machine)
    CUDA_VISIBLE_DEVICES=0
    ```

    **Package `__init__.py` files:**
    Each is a one-liner or empty:
    - `src/__init__.py`: empty
    - `src/models/__init__.py`: `from .schemas import CharacterModel, AssetModel, GenerationJobRequest, ScoringResult`
    - `src/asset_repository/__init__.py`: `from .interfaces import AssetRepository, CharacterRepository; from .sqlite_repo import SQLiteAssetRepository, SQLiteCharacterRepository`
    - `src/generation_engine/__init__.py`: `from .base import GenerationBackend, GenerationInput, GenerationOutput`
    - `src/identity_engine/__init__.py`: `from .scorer import IdentityScorer, ScoringPlugin; from .brand_score import BrandScore`
    - `src/prompt_builder/__init__.py`: `from .templates import PromptTemplates, CharacterPrompt; from .builder import PromptBuilder; from .negative import build_negative_prompt`
    - `src/pipeline/__init__.py`: empty (filled in Plan 01-03)

    **`tests/conftest.py`:**
    Fixtures (per PATTERNS.md):
    - `test_image()` → returns Image.new("RGB", (224,224), color=(255,192,203))
    - `in_memory_db()` → returns SQLiteAssetRepository(db_path=":memory:")
    - `in_memory_char_db()` → returns SQLiteCharacterRepository(db_path=":memory:")
    - `mock_generation_backend()` → returns MockBackend instance (which implements GenerationBackend and returns a white 1024x1024 image with metadata)
    - `lily_character()` → returns CharacterModel(name="Lily Bunny", category="main", species="rabbit", bio_data={"personality":["curious","kind","brave"],"catchphrases":["Let's learn together!","Hi, I'm Lily!"]})
    - `sample_asset(lily_character)` → returns AssetModel(character_id=lily_character.id, asset_type="reference", file_path="/tmp/test.png", scores={"character_consistency":0.85}, brand_score=0.85)

    **`Universe/.gitkeep`:** Empty file to commit the directory structure. The directory tree mirrors PHASE1.md: Characters/, Families/, Friends/, Community/, Fantasy/, StyleGuide/, ColorPalette/, Fonts/, PromptTemplates/, NegativePrompt/, ReferenceSheets/, ModelSheets/.
  </action>
  <verify>
    <automated>pip install -e ".[dev]" 2>&1 | tail -5 && python3 -c "
from src.models import CharacterModel, AssetModel
from src.asset_repository import SQLiteAssetRepository
from src.generation_engine import GenerationBackend
from src.identity_engine import IdentityScorer, BrandScore
from src.prompt_builder import PromptBuilder, build_negative_prompt
print('All packages importable')
"</automated>
  </verify>
  <done>
    - pip install -e ".[dev]" succeeds
    - All six packages import correctly at top level
    - conftest.py fixtures are importable by pytest
    - Universe directory skeleton exists with all subdirectories
    - .env.example documents cloud API key locations
  </done>
</task>

<task type="auto">
  <name>Asset repository tests and schema migration support</name>
  <files>
    tests/test_asset_repository.py,
    src/asset_repository/migrations.py
  </files>
  <action>
    Build the test file for the asset repository and a schema migration helper.

    **`tests/test_asset_repository.py`:**
    Tests for CharacterRepository and AssetRepository:

    - `test_create_character`: save CharacterModel to in-memory SQLite → verify retrieval by id and by name
    - `test_list_characters`: save 3 characters → list → verify all returned
    - `test_create_asset`: save AssetModel → verify retrieval, check default state='draft'
    - `test_update_asset_state`: save asset → update state to 'generated' → verify new state; update to 'scored' → verify
    - `test_find_by_character`: save 2 assets for char A and 1 for char B → find_by_character returns correct counts
    - `test_find_approved`: save 2 assets, approve 1 → find_approved returns only the approved one
    - `test_character_not_found`: attempt to get non-existent character → raises NotFoundError
    - `test_asset_not_found`: attempt to get non-existent asset → raises NotFoundError
    - `test_json_serialization`: verify scores dict and bio_data dict round-trip through SQLite JSON
    - `test_state_transition_validity`: verify asset transitions follow D-15 lifecycle (draft→generated→scored→shortlisted→approved→production→archived); invalid transitions raise ValueError

    Use `@pytest.mark.asyncio` for async tests. Import fixtures from conftest.

    **`src/asset_repository/migrations.py`:**
    `SchemaManager` class with:
    - `__init__(db_path: str)` storing connection path
    - `get_schema_version() -> int`: reads PRAGMA schema_version or custom `_schema_version` table
    - `run_migrations()`: applies versioned SQL migrations in order (initial create, then future alter tables)
    - `_create_schema()`: the CREATE TABLE IF NOT EXISTS statements for characters, assets, jobs, lora_models tables (mirrors RESEARCH.md schema but uses the models/schemas.py conventions)
    - Version tracking via `_schema_version` table with (version INTEGER, applied_at TEXT)

    Critical: Use `sqlite3.connect()` with `?` parameterization for any dynamic values. Never f-string SQL.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_asset_repository.py -x --timeout=30 -v 2>&1</automated>
  </verify>
  <done>
    - All asset repository tests pass (pytest -x green)
    - Schema migration creates all 4 tables (characters, assets, jobs, lora_models)
    - Version table tracks applied migration versions
    - Asset state transitions enforce the D-15 lifecycle correctly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python ↔ SQLite | Process-local database access — no network boundary |
| User ↔ Review UI | Local user interacts with FastAPI review interface (Phase 1: CLI-only, UI in Wave 2) |
| Python ↔ Generation Model | GPU-bound model inference via diffusers or cloud API (model adapters) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | SQLiteAssetRepository SQL queries | high | mitigate | Parameterized queries only (`?` placeholders); no f-string SQL interpolation per RESEARCH.md security domain |
| T-01-02 | Information Disclosure | API error messages | low | mitigate | Use structured `AppError` exception hierarchy; never expose raw stack traces in API responses (Phase 1: CLI only, UI in Wave 2) |
| T-01-03 | Tampering | File path resolution in asset storage | medium | mitigate | Validate `file_path` against allowed base directories using `Path.resolve()` and `Path.relative_to()` |
| T-01-04 | Elevation of Privilege | Database schema initialization | low | accept | SQLite is process-local; only the pipeline process has access |
| T-01-SC | Tampering | pip install of dependencies | high | mitigate | All packages verified in RESEARCH.md Package Legitimacy Audit; `color-palette-extractor` [SUS] flagged — checkpoint:human-verify required before install |

</threat_model>

<verification>
1. Run `pip install -e ".[dev]"` and verify no errors
2. Run `python3 -m pytest tests/test_asset_repository.py -x --timeout=30 -v` — all pass
3. Run the tracer inline verification from Task 1 — all layers connect end-to-end
4. Run `python3 -c "from src.models import *; from src.asset_repository import *; from src.generation_engine import *; from src.identity_engine import *; from src.prompt_builder import *"` — all imports work
5. Verify Universe/ directory structure matches PHASE1.md
</verification>

<success_criteria>
1. ✅ Project installable via `pip install -e ".[dev]"`
2. ✅ Asset repository create/read/update/query works with in-memory SQLite
3. ✅ GenerationBackend ABC with at minimum a MockBackend for testing
4. ✅ IdentityScorer with BrandScore composite works on test images
5. ✅ PromptBuilder composes positive + negative prompts from character templates
6. ✅ End-to-end tracer proves character → prompt → generate → score → save flow
7. ✅ All 12 asset repository tests pass
8. ✅ Schema migration support creates all 4 tables with version tracking
</success_criteria>

<output>
Create `.planning/phases/01-character-universe/01-01-SUMMARY.md` when done
</output>
