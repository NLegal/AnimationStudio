---
phase: 08-music-generation-pipeline-wiring
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
autonomous: true
requirements: []            # ROADMAP Phase 8 "Requirements: TBD" (no REQUIREMENTS.md mapping) — coverage carried by must_haves derived goal-backward from the ROADMAP Phase 8 goal per planning brief
requirements_addressed: []  # same as above
user_setup:
  - service: ACE-Step Studio (local / Colab GPU runtime, OPTIONAL — manual only)
    why: "Real-song generation via the notebook's GPU path or the CLI live smoke; CI and pytest never require it"
    env_vars:
      - name: ACESTEP_API_KEY
        source: "Local ACE-Step Studio deployment configuration (notebook sets a colab-local value on the VM)"
      - name: ACESTEP_BASE_URL
        source: "Default http://localhost:8001 — notebook pins http://127.0.0.1:8001"
    dashboard_config:
      - task: "On Colab: choose a GPU runtime (T4 sufficient for turbo models) before running the service-install cell; models (~10-20 GB) download to the Colab VM on first start, NEVER to Drive"
        location: "Google Colab runtime selector, per the notebook intro cell"
files_modified:
  - src/review_ui/app.py
  - src/review_ui/templates/base.html
  - src/review_ui/templates/music.html
  - tests/test_review_ui_music.py
  - colab/AnimationStudio_Colab_Phase5.ipynb
  - PHASE5_STATUS.md

must_haves:
  truths:
    - "The Review UI serves GET /music rendering the music page with all 24 bible categories, linked from the base navbar; POST /music/prompt re-renders it with caption + negative prompt + resolved LOCKED params computed purely server-side — no database writes and no network calls"
    - "POST /music/generate queues exactly ONE song job per submit through BackgroundTasks + the in-memory JobQueue (job_type 'music'), runs it against an injectable backend, writes the WAV + manifest entry under Audio/Music/ via the SAME script-tier helpers Plan 08-01 shipped, and redirects back to the referer"
    - "GET /api/music/jobs returns the JSON status list for music jobs including error/file enrichment; with the mock backend a submitted job reaches completed (with its WAV on disk) by the time TestClient returns from the POST"
    - "Selecting suno in the UI yields a failed job carrying the refusal message and an HTTP redirect — never a 500 or traceback (C5)"
    - "create_app accepts music_backend=None (lazy get_backend(None) → MUSIC_BACKEND env → mock) and music_dir=None (repo Audio/Music default) so tests inject fakes and tmp dirs"
    - "colab/AnimationStudio_Colab_Phase5.ipynb is valid JSON mirroring the Phase 4 notebook's cell pattern plus one isolated ACE-Step service cell; PHASE5_STATUS.md documents the wiring with working reproduction commands; report mode of generate_phase5.py still exits 0 unchanged (C4)"
  artifacts:
    - path: src/review_ui/app.py
      provides: "create_app(music_backend=None, music_dir=None) DI kwargs; routes GET /music, POST /music/prompt, POST /music/generate, GET /api/music/jobs; context helper _music_page_context(); background worker _run_music_job with function-local src.music_generation imports and MusicBackendError → failed-job boundary"
    - path: src/review_ui/templates/music.html
      provides: "Music page extending base.html: category/params standards table (24 rows), prompt-builder form (pure preview), single-song generate form, recent music jobs panel"
    - path: src/review_ui/templates/base.html
      provides: "Navbar Music link after Motion with nav-active state keyed on page == 'music'"
    - path: tests/test_review_ui_music.py
      provides: "TestMusicPage, TestMusicPrompt, TestMusicGenerate — offline TestClient classes incl. fail-loud transport-guard preview proof and suno failed-job assertion"
    - path: colab/AnimationStudio_Colab_Phase5.ipynb
      provides: "10-cell operator notebook: settings (+RUN_REAL_GENERATION, ACESTEP_MODEL), clone+light install, 24-category preview, isolated ACE-Step service cell (GPU path), --generate run, phase test suites, manifest/report review, PAT-or-download sync, next steps"
    - path: PHASE5_STATUS.md
      provides: "New '## Music Generation Wiring (Phase 8)' section, updated Reproduction block (--generate examples), revised Notes/Caveats audio-rendering claim, deliverables rows for the new artifacts"
  key_links:
    - "_run_music_job imports load_manifest/upsert_entry/atomic_write_manifest/song_filename FROM scripts.generate_phase5 — one shared schema/atomic-write implementation across CLI batch and UI single-song (PATTERNS divergence #2)"
    - "Routes resolve backends exclusively via injected music_backend or lazy get_backend(name); JobQueue stays repo-less (`jq = job_queue or JobQueue()` untouched) so music paths can never touch catalog.db (C2)"
    - "Preview route composes text via build_music_prompt + music_negative_prompt + resolve_music_params — bible-authoritative values, zero re-implementation (Phase 7 locked contract)"
---

<objective>
Wire the Phase 7 music backends into the Review UI — browse page, pure prompt preview, single-song background generation jobs with polling — reusing Plan 08-01's manifest writer helpers verbatim; then ship the operator-facing Colab Phase 5 notebook (offline mock path + isolated ACE-Step GPU path) and bring PHASE5_STATUS.md up to date.

Purpose: Completes the studio-facing half of the ROADMAP Phase 8 goal: an operator can preview and generate songs from the browser without touching the CLI, and can reproduce real generation on Colab following the established notebook family pattern.

Output: modified `src/review_ui/app.py` + `templates/base.html`, new `templates/music.html`, new `tests/test_review_ui_music.py`, new `colab/AnimationStudio_Colab_Phase5.ipynb`, updated `PHASE5_STATUS.md`.
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-music-generation-pipeline-wiring/08-RESEARCH.md
@.planning/phases/08-music-generation-pipeline-wiring/08-PATTERNS.md
@.planning/phases/08-music-generation-pipeline-wiring/08-VALIDATION.md
@.planning/phases/08-music-generation-pipeline-wiring/08-01-SUMMARY.md
@src/review_ui/app.py
@src/review_ui/templates/motion.html
@src/review_ui/templates/base.html
@src/pipeline/job_queue.py
@PHASE5_STATUS.md
</context>

## Hard Constraints (non-negotiable, echo in every commit)

- **C1** — No visual-asset generation anywhere in this phase; no real audio generated in CI/tests (mock backend only; the notebook's real-audio path is operator-machine only).
- **C2** — The repo-root asset-catalog SQLite database is NEVER opened, written, or imported by this phase's code or tests: music routes dereference neither repo nor char_repo, the music JobQueue stays constructed WITHOUT a repository (in-memory only), and UI file writes go ONLY under the configured music directory.
- **C3** — Offline-first: tests use TestClient with injected mock backends and fail-loud transport guards; zero network I/O; the notebook's service cell is documented operator-machine setup that CI never executes.
- **C4** — Report mode of generate_phase5.py stays byte-compatible; the notebook and PHASE5_STATUS Reproduction blocks must only reference commands that actually work.
- **C5** — Suno selection anywhere (UI dropdown, notebook flag) surfaces a typed refusal — failed job + visible message, never HTTP 500 or traceback spill.
- **C6** — Zero new dependencies: pyproject.toml untouched; the notebook installs nothing into the studio env beyond the existing light-deps list and keeps the ACE-Step service in its own isolated checkout/env.

## Sequencing (phase-level)

Execution MUST follow Phase 7 completion AND Plan 08-01 (this plan imports scripts.generate_phase5 manifest helpers). All `src.music_generation` imports are FUNCTION-LOCAL inside routes/handlers per house lazy-import style (PATTERNS S2).

## Artifacts this phase produces (plan 02 share)

Routes inside create_app(): `GET /music` (page), `POST /music/prompt` (pure preview), `POST /music/generate` (single-song job), `GET /api/music/jobs` (JSON polling). Factory kwargs `music_backend=None`, `music_dir=None`. Helpers `_music_page_context(preview=None, form_values=None)` and closure worker `_run_music_job(job_id, category, topic, backend_name)`. Template `music.html`; navbar link in `base.html`. Test module `tests/test_review_ui_music.py` (TestMusicPage, TestMusicPrompt, TestMusicGenerate). Notebook `colab/AnimationStudio_Colab_Phase5.ipynb`. Docs section in `PHASE5_STATUS.md`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 [validation 08-02-01 + 08-02-03]: Tracer — music page, nav link, single-song background job lifecycle end-to-end</name>
  <files>src/review_ui/app.py, src/review_ui/templates/base.html, src/review_ui/templates/music.html, tests/test_review_ui_music.py</files>
  <precondition>Plan 08-01 landed: `python3 -c "import sys; sys.path.insert(0,'.'); from scripts.generate_phase5 import load_manifest, upsert_entry, atomic_write_manifest, song_filename"` exits 0, and Phase 7 surface resolves (`from src.music_generation import get_backend`).</precondition>
  <read_first>
  - .planning/phases/08-music-generation-pipeline-wiring/08-PATTERNS.md — assignment #3 (route/DI excerpts cited by line: create_app signature 231–242, jq line 308, motion trio 951–999, /generate BackgroundTasks 1086–1127, regenerate job creation 1170–1179, /api/jobs shape 688–703, nav 15–19), assignment #4 (template idioms), assignment #5 (test fixtures + background-flush trick), Shared Patterns S1–S7, Deliberate Divergences #1/#2/#3
  - .planning/phases/08-music-generation-pipeline-wiring/08-RESEARCH.md — D2 integration points, Pitfall 3 (never block the request handler), Pitfall 4 (no database coupling), Code Example "Review UI hook trio" (composition sketch)
  - .planning/phases/08-music-generation-pipeline-wiring/08-01-SUMMARY.md — exact manifest-helper names/signatures and filename policy shipped by plan 08-01
  - src/review_ui/app.py — targeted regions above (do NOT re-read the whole file; PATTERNS excerpts suffice)
  - src/pipeline/job_queue.py lines 66–144 — create_job/update_status/list_jobs signatures and validated transitions pending→running→completed/failed
  - src/review_ui/templates/base.html lines 15–19 and templates/motion.html structural skeleton (entity-header, panel sections, form idioms)
  - tests/test_review_ui_api.py fixture/style lines 13–42, 162–175 and tests/test_review_ui_generation.py lines 18–41, 254–264
  </read_first>
  <behavior>
    - GET /music returns 200 HTML containing the page heading, a select listing ALL 24 categories, the resolved params table (Bedtime row shows 66 / F major / 3/4 / 120), and the navbar includes href="/music"; direct navigation marks it nav-active
    - POST /music/generate with data {category: Bedtime, topic: sleepy moon, backend: ""} on a create_app(music_backend=MockBackend(), music_dir=str(tmp)) client returns 303 redirect to the referer; because TestClient flushes background tasks synchronously, the immediately-following GET /api/music/jobs shows that job with status "completed", and tmp contains bedtime-sleepy-moon-0.wav PLUS manifest.json whose songs list grew by one entry written through plan 08-01's helpers
    - POST /music/generate with backend "suno" also returns 303 (NOT 500); the job ends status "failed" with config/error carrying the NotConfigured refusal text citing the research doc path
    - GET /api/music/jobs returns {"jobs": [...]} where each music job exposes id/status/category/error/file fields; image-family jobs from other panels never leak into this list (filter job_type == "music")
    - The music JobQueue remains the factory's repo-less instance: no repository argument reaches it from any music path
    - Fail-loud transport guards installed on the Phase 7 seam stay untripped across every request in this class when the mock backend is injected
  </behavior>
  <action>
Build ONE vertical slice through template → nav → routes → worker → shared writers, then the two test classes proving it. MockBackend and music_dir/tmp_path everywhere; production quality throughout.

1. create_app signature: add music_backend=None and music_dir=None kwargs (docstring entries matching the existing arg-doc style); resolve music_dir default to str(_PROJECT_ROOT / "Audio" / "Music") at factory time. Keep every existing kwarg and default byte-identical.

2. Context helper INSIDE the factory near _motion_page_context: _music_page_context(preview=None, form_values=None) building categories (function-local AudioBible().list_song_categories()), per-category resolved params via lazy-imported resolve_music_params, recent music jobs from jq filtered to job_type == "music" (newest-first), and passthrough preview/form_values dicts. Every src.music_generation import is function-local (S2).

3. Routes INSIDE the factory, mirroring motion/get shapes:
   - GET /music response_class=HTMLResponse renders templates/music.html with context including "page": "music".
   - POST /music/generate accepts Form fields category (default "Alphabet"), topic (default ""), backend (default ""); SINGLE song only — no count/batch parameter exists on this route (divergence #1, DoS cap T8-03-D). Create the job via jq.create_job(character_id="music", job_type="music", config={"category": category, "topic": topic, "backend": backend}), update_status to running, background_tasks.add_task(_run_music_job, job.id, category, topic, backend), logger.info the queueing, RedirectResponse(_get_referer(request), 303).
   - GET /api/music/jobs returns {"jobs": [{id, status, category, error, file}]} built from jq.list_jobs() filtered to job_type == "music", enriching error/file from j.config.get(...) ("" defaults), created_at isoformat like /api/jobs.

4. Closure worker async def _run_music_job(job_id, category, topic, backend_name): function-local imports of get_backend/build_music_request/MusicBackendError AND of the plan 08-01 script helpers (load_manifest, find_entry, upsert_entry, atomic_write_manifest, song_filename); resolve backend = music_backend if provided else get_backend(backend_name or None); result = backend.generate(build_music_request(category, topic)); write bytes to os.path.join(music_dir, song_filename(category, topic, result.seed, result.format)); upsert into load_manifest(os.path.join(music_dir, "manifest.json")) and atomic_write_manifest; record config["file"] = filename and update_status completed. Wrap the whole body in except MusicBackendError as exc → logger.error("Music job %s failed: %s", job_id, exc) (feeds the dashboard activity buffer), config["error"] = str(exc), update_status failed — NEVER let it escape to the framework (C5).

5. Template src/review_ui/templates/music.html extending base.html using motion.html idioms ONLY ({@ext panel/dashboard-table/btn/prompt-result classes}): entity-header with Back link + h1 + phase meta; standards panel looping the 24 categories (name/BPM/key/time-signature/duration columns); single-song generate form posting to /music/generate (category select over all categories, topic text input sticky via form_values.get, backend select offering ace-step/suno/mock plus an empty default labeled env-default); recent jobs panel rendering id/status/category/error/file with an empty-state fallback. NO JavaScript required (motion-page parity).

6. Nav: insert the Music anchor between Motion and the badge span in templates/base.html keyed on page == 'music' nav-active. Leave the badge text untouched.

7. Tests tests/test_review_ui_music.py — module docstring naming covered surfaces; lightest client fixture per assignment #5 (plain create_app(db_path=str(tmp_path / "m.db"), music_backend=MockBackend(), music_dir=str(tmp)) — stub repos unnecessary since music routes ignore them); env-isolation delenv of MUSIC_BACKEND/ACESTEP_* in fixtures; fail-loud seam guards around every request. Classes: TestMusicPage (render/nav/categories/params-table assertions) and TestMusicGenerate (the four POST/poll behaviors above incl. suno-failed-job-not-500 and the completed-job file+manifest assertions).

Commit the tracer atomically before Task 2.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_review_ui_music.py -x -q && python3 -m pytest tests/test_review_ui_music.py::TestMusicGenerate -q && python3 -m pytest tests/test_review_ui_api.py tests/test_review_ui_generation.py -q</automated>
  </verify>
  <acceptance_criteria>
  - `python3 -m pytest tests/test_review_ui_music.py -x -q` exits 0
  - Completed-job assertion proves the full loop: 303 redirect → job completed → WAV on disk in tmp music_dir → manifest.json gained exactly one thirteen-key entry via the SHARED script-tier helpers (import proven by monkeypatching atomic_write_manifest in one test and observing the call)
  - Suno submit returns 303 with terminal job status "failed" and refusal text present in /api/music/jobs output; response body/status is never a 500 (constraint C5 secure behavior)
  - /api/music/jobs payload shape asserted key-by-key; non-music jobs excluded by the filter
  - Existing review-ui suites still green (regression guard on the factory signature change): test_review_ui_api + test_review_ui_generation pass unchanged
  - Fail-loud guards untripped across the whole module (constraint C3)
  - `! rg -q "sqlite3" src/review_ui/templates/music.html tests/test_review_ui_music.py` (constraint C2 — static gate; app.py adds no new driver import either)
  - `git diff --exit-code pyproject.toml` (constraint C6)
  </acceptance_criteria>
  <reversibility rating="reversible">Additive routes behind default-None DI kwargs; removing them restores prior behavior; the cross-tier import replaces duplication, not architecture.</reversibility>
  <done>A browser operator can open /music from the navbar, submit one song, watch it complete via the polling endpoint, and find the WAV + manifest entry on disk written by the same code path as the CLI — with suno degrading gracefully — all committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 [validation 08-02-02]: POST /music/prompt — pure bible-conformant preview</name>
  <files>src/review_ui/app.py, src/review_ui/templates/music.html, tests/test_review_ui_music.py</files>
  <read_first>
  - .planning/phases/08-music-generation-pipeline-wiring/08-PATTERNS.md — pure-preview POST excerpt (app.py 958–999) verbatim including the docstring purity guarantee, plus motion.html prompt-builder/result-block markup idioms (lines 53–100)
  - .planning/phases/08-music-generation-pipeline-wiring/08-RESEARCH.md — D2 preview bullet (request_json/caption/negative/params payload), Validation Architecture row for /music/prompt (guards never trip; golden params)
  - .planning/phases/07-music-generation-backend-integration/07-01-PLAN.md Task 2 — LOCKED golden param table (Bedtime 66/F major/3/4/instrumental-intro scaffold/120 s) the preview must echo
  - src/audio_bible/prompts.py — build_music_prompt/category_negative signatures (preview composes via these; zero re-implementation)
  - tests/test_review_ui_api.py TestMotionPromptBuilder (lines ~407–427) — form-preservation assertion style to mirror
  </read_first>
  <behavior>
    - POST /music/prompt with {category: Bedtime, topic: sleepy moon} returns 200 re-rendering music.html whose prompt-result block shows: request_json (pretty-printed MusicRequest dump with duration_s 120), caption produced by build_music_prompt containing the category keyword "lullaby", negative prompt from music_negative_prompt (non-empty, includes base negatives), and params echoing bpm 66 / F major / 3/4
    - Form values are preserved post-submit: the category option selected matches the submission and the topic input carries the submitted topic (sticky-form idiom)
    - Default topic applies when blank: submitting empty topic previews "{category.lower()} fun" in both the request JSON and the sticky field
    - Purity proof: with fail-loud raisers monkeypatched onto the Phase 7 transport seam functions, the POST succeeds — zero network I/O on the preview path
    - Unknown category submitted via crafted form data does not 500: generic fallback params render (resolve_music_params case-insensitive generic row) — or the select constrains input; behavior is graceful either way and asserted
  </behavior>
  <action>
Add the pure-preview route and its template section, mirroring the motion pair exactly. Extend TestMusicPrompt first, then implement.

1. Route INSIDE the factory directly beneath GET /music, docstring stating the purity guarantee in the motion-prompt style ("Pure evaluation of the bible prompt builders — no database writes and no network calls"). Parse form via str(form.get(...)).strip() with inline defaults (category "Alphabet", topic "" → f"{category.lower()} fun"); function-local imports of build_music_request, resolve_music_params, music_negative_prompt, and src.audio_bible.prompts.build_music_prompt; assemble preview dict {"request_json": req.model_dump_json(indent=2), "caption": ..., "negative": ..., "params": params.model_dump()}; re-render music.html via _music_page_context(preview=..., form_values={"category": ..., "topic": ...}).

2. Template section in music.html placed BEFORE the generate form: prompt-builder panel titled like the motion analog with its own form action="/music/prompt" method="post" (category select + topic input, sticky values), and a conditional {% if preview %} result block rendering caption paragraph, negative-prompt paragraph (prompt-negative class), params line, and the request_json inside a pre block.

3. Class TestMusicPrompt in tests/test_review_ui_music.py covering every <behavior> bullet: golden Bedtime values, keyword-substring caption check, sticky-field assertions in raw HTML, blank-topic default, transport-guard purity proof, and the unknown-category graceful-render case. Keep the client fixture identical to Task 1 (injected mock backend irrelevant here but harmless).

Run the phase quick command, then FULL suite wave-gate. Commit after green.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_review_ui_music.py::TestMusicPrompt -q && python3 -m pytest tests/test_review_ui_music.py -q && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - `python3 -m pytest tests/test_review_ui_music.py::TestMusicPrompt -q` exits 0
  - Golden LOCKED params asserted in rendered HTML for Bedtime (66 BPM / F major / 3/4 / duration_s 120) and caption keyword "lullaby" present
  - Sticky-form assertions prove category/topic survive the POST round-trip; blank topic falls back to the lowercase-fun convention
  - Purity proven: transport-seam fail-loud raisers never trip during any preview request (secure behavior row 08-02-02)
  - Unknown-category submission renders generic fallback params with HTTP 200 — no exception escapes
  - Full-suite gate green modulo the 5 known story-engine failures
  </acceptance_criteria>
  <done>Operators can compose and inspect a bible-conformant song prompt (caption, negatives, tempo/key/meter, request JSON) entirely server-side with provably no side effects — the decision point before committing compute to real generation.</done>
</task>

<task type="auto">
  <name>Task 3 [validation 08-02-04]: Colab Phase 5 notebook + PHASE5_STATUS.md wiring docs</name>
  <files>colab/AnimationStudio_Colab_Phase5.ipynb, PHASE5_STATUS.md</files>
  <precondition>`test -f colab/AnimationStudio_Colab_Phase4.ipynb` succeeds (structural template present) and report mode works: `python3 scripts/generate_phase5.py --docs-dir Audio --out /tmp/opencode/phase8-report-compat.md` exits 0.</precondition>
  <read_first>
  - .planning/phases/08-music-generation-pipeline-wiring/08-PATTERNS.md — assignment #6 (Phase 4 notebook cells 1/2/7 quoted verbatim + S8 conventions) and caveat rows for the no-analog ACE-Step service cell (isolated venv MANDATORY per Pitfall 7)
  - .planning/phases/08-music-generation-pipeline-wiring/08-RESEARCH.md — D3 cell table (exact 10-cell layout), ACE-Step service facts (CITED vendor README install/launch/health commands, VRAM numbers), D4 PHASE5_STATUS update targets, State of the Art deprecated rows (legacy v1 pip package must NOT be used)
  - .planning/phases/08-music-generation-pipeline-wiring/08-VALIDATION.md — Manual-Only Verifications row wording + the structural verification command this task must satisfy
  - colab/AnimationStudio_Colab_Phase4.ipynb — read cells 0/3/5/6/8 sources (cells 1/2/7 already quoted in PATTERNS) to mirror tone/structure
  - colab/git_sync.py lines 25–33 — _basic_auth_header reference for the sync cell
  - PHASE5_STATUS.md — FULL read (84 lines): deliverables table format (lines 8–20), Reproduction block (49–60), Notes/Caveats stale claim (~82–84), dated header (3–4)
  </read_first>
  <behavior>
    - python3 -c "import json;json.load(open('colab/AnimationStudio_Colab_Phase5.ipynb'))" exits 0 (valid nbformat JSON)
    - Notebook contains exactly the 10 planned cells in order: markdown intro; settings (@param incl. RUN_REAL_GENERATION boolean + ACESTEP_MODEL choice); clone+install (pip -e . --no-deps + light deps, closing print explaining CPU vs GPU paths); 24-category preview via AudioBible().list_song_categories(); ACE-Step service install/start cell (GPU path: clone /content/ACE-Step-1.5, isolated uv sync, uv run acestep-api, health-wait loop, pip-fallback comment); generation run cell invoking scripts/generate_phase5.py --generate --backend acestep|mock gated on RUN_REAL_GENERATION; test-suite cell (pytest tests/test_audio_bible.py tests/test_music_generation.py -q); review cell displaying manifest.json summary + report; sync cell staging Audio/Music/ and PHASE5_REPORT.md with PAT basic-auth push or files.download fallback; markdown next-steps
    - No notebook cell installs anything into the studio env beyond the Phase 4 light-deps list; the service cell comments state the isolated-env rationale and the never-to-Drive model policy
    - Every command the notebook shells out to exists in the repo and is itself tested (script flags, pytest files)
    - PHASE5_STATUS.md gains a '## Music Generation Wiring (Phase 8)' section with a deliverables/evidence table (script mode, UI hooks, notebook, Audio/Music/manifest.json inventory role, test counts), Reproduction block extended with the offline mock --generate example and live-smoke pointer, Notes/Caveats audio-rendering sentence rewritten to describe wired generation with backend selection, dated header refreshed
    - Report-mode reproduction still passes verbatim: python3 scripts/generate_phase5.py exits 0 (C4 evidence)
  </behavior>
  <action>
Author the notebook as valid nbformat v4 JSON (nbformat: 4, metadata mirroring the Phase 4 kernel spec; each cell with correct cell_type/source-list form) and update the status doc. This task is documentation-grade — verify structurally, never execute the GPU path.

1. Build the 10-cell notebook per the D3 table, copying Phase 4 cell sources verbatim where PATTERNS marks them exact (settings/clone-install/sync skeletons adapted minimally: stage Audio/Music/ alongside the report in the sync cell; keep #@title numbering contiguous). New content cells follow RESEARCH lines ~302–330 for the service cell (clone ACE-Step-1.5 to /content, isolated dependency sync via uv with inline pip fallback note, launch acestep-api pinned to 127.0.0.1:8001, bounded health-wait loop with generous first-start budget for model download, ACESTEP_* exports scoped to the notebook process) — include CITED VRAM guidance (turbo 4.7 GB weights comfortable on T4; XL needs offload) as comments and the explicit statement that models download to the Colab VM, never Drive. Generation cell branches on RUN_REAL_GENERATION: True → --backend ace-step (via the accepted alias spelling used elsewhere in the repo docs, noting acestep equivalence), False → --backend mock offline demo writing into Audio/Music/. Keep every shell-out target limited to commands already verified in-repo.

2. Update PHASE5_STATUS.md with Edit-scoped changes only: refresh the dated verification line; append four deliverables rows (generation-mode script, Review UI music hooks, Colab Phase 5 notebook, Audio/Music/manifest.json artifact inventory); extend the Reproduction fenced bash with the mock one-liner (--generate --backend mock --out Audio/Music) and a comment pointing at the README live-smoke checklist; add the new '## Music Generation Wiring (Phase 8)' section summarizing tier responsibilities (script batch / UI single-song / notebook orchestration) with the test-count evidence placeholder filled from the actual suite; rewrite the stale audio-rendering caveats sentence to state that generation now wires through get_backend with mock default and ace-step local-service option, suno refusing pending an official API.

3. Verification hygiene: run the VALIDATION structural command (JSON validity + report-mode run below); grep the notebook source strings to confirm the legacy v1 pip package name appears NOWHERE (State of the Art deprecation) and that no cell mutates pyproject.toml.

Commit after green. Run the FULL suite once more as the plan-closing gate.
  </action>
  <verify>
    <automated>python3 -c "import json;json.load(open('colab/AnimationStudio_Colab_Phase5.ipynb'))" && python3 -m pytest tests/test_review_ui_music.py -q && python3 scripts/generate_phase5.py --docs-dir Audio --out /tmp/opencode/phase8-final-report.md && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - Notebook parses as valid JSON; cell census == 10 in the D3 order; settings cell declares RUN_REAL_GENERATION and ACESTEP_MODEL @param fields
  - Service cell clones the 1.5 GitHub repo (not the deprecated PyPI v1 package), uses an isolated environment step, and pins the 127.0.0.1:8001 base URL consistent with ACESTEP_BASE_URL defaults
  - Sync cell stages Audio/Music/ together with PHASE5_REPORT.md and preserves the PAT-header/download-fallback duality
  - PHASE5_STATUS.md contains the new section heading, four new deliverables rows, the --generate mock reproduction line, and NO stale claim that audio rendering is left entirely external
  - `python3 scripts/generate_phase5.py --docs-dir Audio --out /tmp/opencode/phase8-final-report.md` exits 0 — Reproduction commands remain truthful (C4)
  - Full-suite gate green modulo the 5 known story-engine failures — phase complete signal for /gsd-execute-phase
  </acceptance_criteria>
  <reversibility rating="reversible">New operator artifact + additive doc edits; both revert cleanly and gate on no runtime behavior.</reversibility>
  <done>The notebook family gains its Phase 5 member mirroring the established operator pattern with a clearly-isolated GPU path, and PHASE5_STATUS.md truthfully documents the now-wired generation pipeline — closing the final ROADMAP Phase 8 deliverables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ FastAPI routes | Untrusted form fields (category/topic/backend) cross into request building and filenames |
| process ↔ local ACE-Step service | Reached only via Phase 7 adapter; UI adds no parsing |
| notebook ↔ Colab VM | Service clone/install executes vendor commands on the operator's VM — explicitly NOT studio deps |
| tests ↔ world | Zero network, zero real audio, zero database side effects |

## STRIDE Threat Register (ASVS L1; block on unresolved high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T8-01 | Tampering/Elevation of Privilege | form-supplied category/topic reaching filenames | high | mitigate | UI worker funnels BOTH components through plan 08-01's slugify/filename helpers (single choke point); traversal unit coverage inherited; unknown-category form input degrades to generic params, never arbitrary paths |
| T8-03-D | Denial of Service | unbounded batch via UI request | high | mitigate | POST /music/generate handles EXACTLY one song per submit — no count/scope parameters exist on the route; per-song work capped by protocol timeout_s=300; batches belong to CLI/notebook (divergence #1); dedicated test asserts the route ignores extraneous batch-style fields |
| T8-04-C | Tampering (data integrity) | repo-root asset-catalog database | high | mitigate | Music routes dereference neither repo nor char_repo; JobQueue stays repo-less; grep-gate over new phase files plus existing review-ui suite regression run |
| T8-02-I | Info Disclosure | logs/activity buffer/job payloads | medium | mitigate | Failure messages carry typed exception text only (refusal reasons cite the research doc, not env contents); no credential-bearing values enter config dicts; caplog-style assertion in TestMusicGenerate checks the suno failure path emits no secret-looking material |
| T8-SC | Tampering (supply chain) | notebook service cell + dependency set | high | mitigate | Zero project-dependency installs (pyproject diff-gate); service cell clones the OFFICIAL ACE-Step-1.5 repo per vendor-CITED URL into an ISOLATED env on the operator VM — flagged operator-machine setup, never CI |
| T8-S | Spoofing | unauthenticated localhost routes | low | accept | Single-operator tool at ASVS L1; matches every existing review-ui route disposition |
| T8-R | Repudiation | UI-initiated generations | low | accept | JobQueue records + activity log buffer provide traceability adequate at L1 |
| T8-E | Elevation of Privilege | worker file writes | low | accept | Writes confined to the configured music directory via shared helpers; no subprocess/eval introduced by routes |
</threat_model>

<verification>
- `python3 -m pytest tests/test_review_ui_music.py -q` green (phase quick command from 08-VALIDATION.md)
- `python3 -m pytest tests/ -q` green modulo the 5 known story-engine catalog failures (~152 s ceiling)
- `python3 -c "import json;json.load(open('colab/AnimationStudio_Colab_Phase5.ipynb'))"` exits 0; `python3 scripts/generate_phase5.py --docs-dir Audio --out /tmp/opencode/phase8-report-compat.md` exits 0 (C4)
- Constraint gates: no new database-driver imports in phase files; music jobs repo-less; pyproject.toml untouched; UI paths dial-free under fail-loud guards
- Suno degradation verified at BOTH entry points (CLI in plan 08-01, UI here) — never a 500/traceback
</verification>

<success_criteria>
- All six truths in must_haves hold, evidenced by TestMusicPage / TestMusicPrompt / TestMusicGenerate plus structural notebook checks
- UI submits are single-song with polling status; CLI batching remains the batch surface (research Open Question 2 recommendation adopted)
- Manifest writes across tiers share ONE implementation (scripts.generate_phase5 helpers imported by the worker)
- Notebook mirrors the Phase 4 family pattern with the isolated ACE-Step service cell; PHASE5_STATUS.md claims match actual artifacts
- Zero new dependencies; repo-root asset-catalog database untouched; full suite green modulo known failures
</success_criteria>

<output>
Create `.planning/phases/08-music-generation-pipeline-wiring/08-02-SUMMARY.md` when done (record the final /api/music/jobs JSON shape, the music_dir DI resolution, and the notebook cell census for the phase verifier).
</output>
